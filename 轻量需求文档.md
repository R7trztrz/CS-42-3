# CS-42 需求开发文档（轻量版）

> **范围**：本文档仅收录指导开发所需的需求内容——参与者定义、模块划分、用例清单、功能/非功能需求。背景、技术方案与决策理由不在此重复，见关联文档。
>
> **关联文档**：
> - `决策依赖矩阵.md`——客户待定项与默认假设（[D-x]）
> - `技术栈定案草案.md` / `Tech Stack Decision.md`——技术选型与理由
> - 技术架构文档——系统结构与关键机制
>
> **约定**：需求 ID 前缀——UC（用例）/ FR（功能需求）/ NFR（非功能需求）；每条需求需可追溯到对应用例与验收标准。

## 1. 模块划分

按"研究生命周期 + 用户角色"将系统划分为 **7 个模块（1 个横切 + 6 个业务）**，与既有后端功能域、前端应用及成员分工对齐：

| # | 模块 | 范围 | 主要责任人 |
|---|---|---|---|
| M1 | **账号与权限**（横切） | 研究者注册/登录/登出、RBAC、会话安全（JWT） | 网安 ×2 |
| M2 | **研究管理** | 研究 CRUD、发布/关闭、邀请链接、被试者参与总览/进度/详情查看 | 软工 |
| M3 | **信息流编辑** | craft.js 编辑器外壳：模板（7 平台 + 空白）、控件库、属性面板、草稿保存 | 交互（软工配合适配层） |
| M4 | **问卷（题库 + 问卷）** | 题库管理（账号级：导入/维护题目）+ 问卷定义（研究级：选题、顺序、跳转逻辑） | 交互 + 算法 |
| M5 | **参与会话与问卷运行** | 进入（链接/token）、知情同意、进度与续传、作答、分支运行时 | 交互 + 算法/软工 |
| M6 | **行为采集与眼动** | 采集层（点击/鼠标/打字/滚动/缓冲上报）+ 眼动（标定、每帧采样、控件归因、可用性记录） | AI（交互嵌入标定页） |
| M7 | **数据导出** | CSV/zip 导出、异步任务、完整性校验 | 软工 |

> **范围注记（v1 边界）**：数据导出为**确定需求**（M7）；**v1 不展示任何研究数据内容**（问卷作答、行为/眼动数据均不在界面展示——展示需要额外查询与字段设计），研究数据的出口统一为导出；**数据可视化（图表/统计视图等）不在 v1**（P1 可选）。研究者端在 v1 仅展示运营性信息（研究状态、会话状态、匿名进度），下钻链路：参与总览 → 进度列表（UC-12）→ 被试者会话信息 + 导出入口（UC-13）→ 导出（M7）。

## 2. 参与者定义（Actors）

| 参与者 | 定义 | 使用系统部分 | 认证方式 | 主要目标 |
|---|---|---|---|---|
| **Researcher（研究者）** | 设计并开展研究的用户（研究团队成员）。维护个人题库，创建与管理研究，编辑模拟信息流与问卷内容，发布研究，管理被试者访问并导出数据。角色字段为 `researcher`（未来如需管理员等更细角色可扩展，本文档不假设）。 | researcher-dashboard | 账号密码登录 + JWT（Spring Security 校验，RBAC 授权） | 搭建并运行一项社交媒体行为研究，获取可供分析的数据 |
| **Participant（被试者）** | 通过研究者分发的邀请链接/token 匿名进入研究的用户。无需注册；在明确同意后浏览模拟信息流并作答问卷；其交互行为与（启用时）眼动数据被系统采集。 | participant | 匿名 token/邀请码（非 JWT，不建立账号） | 在模拟社交媒体环境中按研究设计完成浏览与作答 |
| **System（系统）** | 自动执行、无直接用户操作的行为主体——行为与眼动数据的后台采集与上报、分支路由执行、眼动可用性判定、数据校验等。 | 后端服务 + 参与端采集层 | —（内部机制） | 在无人干预下可靠地采集、处理与保护研究数据 |

---

## 3. 用例清单

> 约定：用例按模块逐个确认后追加；编号全局顺序（UC-xx），每条含所属模块。格式：目标 | 参与者 | 主流程 | 备选流程 | 验收标准。标注"暂不实现/待确认"的条目不阻塞开发，按决策矩阵转正。

### M1 账号与权限

#### UC-01 研究者注册

- 模块：M1｜参与者：Researcher、System
- 目标：研究者创建账号，以便使用研究者端。
- 主流程：
  1. 研究者打开注册页；
  2. 系统显示注册表单（邮箱、密码、确认密码）；
  3. 研究者提交注册信息；
  4. 系统校验：邮箱格式合法且未被注册；两次密码一致且满足强度要求（如 ≥8 位，具体策略实现时定）；
  5. 系统创建账号（默认角色 researcher，密码以 BCrypt 哈希存储）；
  6. 系统引导研究者登录（或自动登录）。
- 备选流程：
  - 4a 邮箱已注册：提示"该邮箱已注册"，引导至登录页；
  - 4b 密码不一致或强度不足：表单错误提示，不创建账号。
- 验收标准：
  1. 注册成功后可用新账号执行 UC-02 登录；
  2. 重复邮箱注册被拒绝且不泄露账号细节；
  3. 密码不以明文存储（BCrypt），任何接口不返回密码。
- 备注：账号创建方式默认**自助注册**（注册即 researcher 角色）；若客户要求仅内部研究团队可用，改为团队预置账号（本用例删除，另设"预置账号首次设置密码"）——实现前与客户确认。

#### UC-02 研究者登录

- 模块：M1｜参与者：Researcher、System
- 目标：研究者凭账号密码进入研究者端并取得操作权限。
- 主流程：
  1. 研究者打开研究者端并选择"登录"；
  2. 系统显示登录表单（邮箱 + 密码）；
  3. 研究者提交凭证；
  4. 系统校验账号存在且密码正确（BCrypt 比对）；
  5. 系统签发 JWT（含 userId、role、过期时间）并返回前端；
  6. 研究者进入 dashboard，后续请求携带 JWT。
- 备选流程：
  - 3a 凭证错误：提示统一错误信息（"邮箱或密码错误"，不区分具体哪项，防账号枚举），停留在登录页。
- 验收标准：
  1. 合法凭证登录成功并进入 dashboard；
  2. 非法凭证登录失败且错误提示不区分"账号不存在/密码错误"；
  3. 携带有效 JWT 的请求可通过；过期或伪造 JWT 被拒绝（401）；
  4. 密码以 BCrypt 哈希存储，任何接口不返回明文密码。
- 备注：会话有效期与续期策略归 NFR。

#### UC-03 研究者登出

- 模块：M1｜参与者：Researcher
- 目标：研究者结束会话，本地凭证失效。
- 主流程：
  1. 研究者点击"登出"；
  2. 系统清除前端保存的 JWT；
  3. 系统跳转回登录页。
- 验收标准：
  1. 登出后访问受保护页面被拒绝并重定向至登录页（401）；
  2. 登出仅需清除前端凭证。

#### UC-04 研究者访问受保护功能（权限控制）

- 模块：M1（横切）｜参与者：Researcher、System
- 目标：只有 researcher 角色的研究者能执行受保护操作，且只能访问属于自己的资源。
- 主流程：
  1. 研究者发起受保护操作（如创建/发布研究、导出数据）；
  2. 系统校验请求携带的 JWT（验签 + 未过期）；
  3. 系统校验角色为 researcher（RBAC，URL/方法级规则）；
  4. 系统校验资源归属（目标研究/题库属于当前研究者）；
  5. 操作执行。
- 备选流程：
  - 2a 无/无效/过期 JWT → 返回 401 未认证；
  - 3a 角色不符 → 返回 403 无权限；
  - 4a 资源不属于当前研究者 → 拒绝访问（403/404 返回策略见下方说明，实现时定）。
- 验收标准：
  1. 未登录访问受保护端点返回 401；
  2. 无 researcher 角色的请求被拒绝（403）；
  3. 研究者无法查看或操作他人的研究及题库数据；
  4. 参与链接对应的公开端点（无需 JWT）不受本用例约束。
- 备注：本用例为横切约束——后续各模块用例中的"研究者受保护操作"默认继承本用例的认证与授权要求，不再重复描述。

> **具体返回策略实现**：被拒绝是肯定的，但返回哪个 HTTP 状态码有两种策略，各有安全权衡：
>
> | 策略 | 返回 | 效果 |
> | --- | --- | --- |
> | 诚实式 | **403 Forbidden** | "你已认证，但这个资源不是你的"——攻击者可通过对比区分"资源存在但不是我的"（403）和"资源不存在"（404），从而探测资源存在性 |
> | 隐身式（更推荐） | **404 Not Found** | 把"不是你的"伪装成"不存在"——不泄露资源是否存在，防枚举 |

#### UC-05 研究者修改密码

- 模块：M1｜参与者：Researcher
- 目标：研究者登录后修改自己的账号密码。
- 主流程：
  1. 研究者进入账号设置并选择"修改密码"；
  2. 系统显示表单（当前密码、新密码、确认新密码）；
  3. 研究者提交；
  4. 系统校验当前密码正确（BCrypt 比对）；
  5. 系统校验新密码满足强度要求（与 UC-01 注册一致）；
  6. 系统更新密码哈希，提示修改成功。
- 备选流程：
  - 4a 当前密码错误：提示错误，不更新密码；
  - 5a 新密码强度不足或两次输入不一致：表单错误提示。
- 验收标准：
  1. 当前密码错误时拒绝修改；
  2. 新密码强度要求与 UC-01 注册一致；
  3. 修改成功后旧密码立即失效（用旧密码登录被拒绝）；
  4. 密码仍以 BCrypt 哈希存储。
- 备注：修改成功后是否要求重新登录（使旧 JWT 失效）由会话策略决定（归 NFR）。

#### UC-06 忘记密码 / 自助重置（暂不实现，保留记录）

- 模块：M1｜参与者：Researcher｜状态：**v1 不做（记录在案）**
- 目标：研究者忘记密码时自助重置（常规实现：邮箱重置链接）。
- 暂不考虑做的原因：
  1. **依赖邮件服务**：标准实现需要 SMTP 发送重置链接，项目当前无邮件基础设施，引入即新增范围与运维负担；
  2. **无管理员角色**：备选方案"管理员代重置"依赖 admin 角色，本文档明确不假设该角色；
  3. **用户规模小**：平台面向内部研究团队与少量研究者账号，非开放公众平台，忘记密码属小概率事件。
- v1 兜底：研究者忘记密码时由团队线下重置（维护期直接操作），成本近似为零。
- 转正条件（P1 可选）：客户明确要求自助重置 → 评估引入邮件服务（或等效安全方案）后按需实现。
- 验收标准（实现时补充）：此处仅记录决策，不设验收标准。

### M2 研究管理

#### UC-07 创建研究

- 模块：M2｜参与者：Researcher
- 目标：研究者创建一个新的研究，作为后续内容编辑与数据收集的容器。
- 主流程：
  1. 研究者点击"新建研究"；
  2. 系统显示创建表单（标题、描述）；
  3. 研究者填写并提交；
  4. 系统创建研究（初始状态：草稿，归属当前研究者）；
  5. 系统进入研究工作区（可开始编辑内容 / 配置设置）。
- 验收标准：
  1. 创建成功后研究出现在"我的研究"列表中，状态为草稿；
  2. 研究归属为创建它的研究者（受 UC-04 横切约束）。
- 备注：研究的完整内容（信息流/问卷）由 M3/M4 产出，本用例只建立研究容器。

#### UC-08 查看与编辑研究

- 模块：M2｜参与者：Researcher
- 目标：研究者查看自己的研究列表/详情，并修改研究基本信息与运行设置。
- 主流程：
  1. 研究者进入"我的研究"列表；
  2. 系统显示其名下的研究（标题、状态：草稿/收集中/已关闭、创建时间）；
  3. 研究者打开某研究详情并编辑基本信息（标题、描述）或运行设置；
  4. 系统保存修改。
- 备选流程：
  - 3a 研究已发布（收集中）：基本信息修改仅对后续访问生效，不影响已发布快照；运行设置（如是否启用眼动）是否允许运行中修改，实现时定（默认允许，仅对新会话生效）。
- 验收标准：
  1. 列表只显示当前研究者的研究（UC-04 归属约束）；
  2. 草稿状态的研究信息可自由修改；
  3. 已发布研究的修改不影响已发布版本内容（版本化发布，见 M3；问卷锁定规则见 M4）。
- 备注：运行设置项清单（眼动启用、浏览时长等）随 M3–M6 需求变化，本用例只定义"可查看与编辑"的能力。

#### UC-09 发布研究

- 模块：M2｜参与者：Researcher、System
- 目标：研究者将草稿研究发布为可参与的正式版本，并取得参与链接。
- 主流程：
  1. 研究者点击"发布"；
  2. 系统校验发布条件：信息流文档已定义且非空；问卷已配置（已从题库启用题目并保存跳转逻辑）；
  3. 系统将当前内容固化为 published 快照——**问卷在本次发布时固化（此后锁定，见 M4 模块说明）**；信息流作为草稿-快照体系的一部分固化；
  4. 系统生成参与链接（含匿名 token）并将研究状态置为"收集中"；
  5. 系统向研究者展示参与链接（可复制）。
- 备选流程：
  - 2a 内容不完整：提示缺失项（如"尚未配置问卷"），阻止发布；
  - 4a 重复发布（研究已收集中）：**仅更新信息流内容为新快照（信息流支持版本化再次发布）；问卷已锁定，不可随重新发布变更**（如需变更问卷内容，须新建研究或删除重建——简化决策，见 M4 模块说明）。
- 验收标准：
  1. 发布后参与者通过链接访问到的是不可变的 published 快照内容；
  2. 发布后草稿的继续修改不影响已发布版本；问卷在发布后不可修改（编辑入口关闭并提示）；
  3. 未满足发布条件时发布被阻止并给出明确提示；
  4. 发布后研究状态为"收集中"，参与者可进入（见 M5 参与会话用例）。
- 备注：发布动作对信息流与问卷的固化语义不同——信息流可再次发布更新快照；问卷一经发布锁定（进入使用阶段即不可修改，简化开发）。已采集数据与版本快照的关联由数据模型保证（见技术架构）。

#### UC-10 关闭 / 重新开放研究

- 模块：M2｜参与者：Researcher
- 目标：研究者停止或恢复研究的参与入口。
- 主流程：
  1. 研究者选择"关闭研究"；
  2. 系统确认操作；
  3. 系统将研究状态置为"已关闭"，参与链接失效（新会话不可创建）；
  4. （可选）研究者选择"重新开放"→ 状态回到"收集中"，链接恢复可用。
- 备选流程：
  - 进行中的会话：关闭后进行中的会话不可提交。
- 验收标准：
  1. 关闭后通过原链接无法创建新会话；
  2. 重新开放后链接恢复可用；
  3. 已采集数据不受关闭/开放影响。
- 备注：状态机为 草稿 → 收集中 ⇄ 已关闭 → 删除。重新开放已默认允许；如客户不希望，改状态机即可。

#### UC-11 删除研究

- 模块：M2｜参与者：Researcher
- 目标：研究者删除不再需要的研究。
- 主流程：
  1. 研究者选择"删除研究"；
  2. 系统提示后果并请求确认（该研究及其全部数据将一并删除）；
  3. 研究者确认；
  4. 系统删除研究及其关联数据（文档快照、作答、行为/眼动事件、会话）。
- 备选流程：
  - 2a 研究者取消：不执行删除。
- 验收标准：
  1. 删除前必须二次确认；
  2. 删除后研究从列表消失，关联数据不可再通过系统访问；
  3. 只有归属研究者能删除（UC-04）。
- 备注：**删除已采集数据为不可逆破坏性操作**——v1 默认允许级联删除；若客户要求"删除研究但保留原始数据"，需改为软删除策略（保留数据、仅移除研究入口）——待客户确认或数据保留政策明确后调整。删除研究不影响研究者题库（题库为账号级资源，见 M4）。

#### UC-12 查看研究参与总览与进度列表

- 模块：M2｜参与者：Researcher
- 目标：研究者总览某研究的所有参与者情况，并作为进入单个被试者详情（UC-13）的入口。
- 主流程：
  1. 研究者进入某研究的"参与情况"页；
  2. 系统显示参与总览（总进入数、完成数、进行中、放弃数）；
  3. 研究者查看进度列表（匿名被试者标识、进入时间、状态、完成时间）；
  4. 研究者点击列表中的某位被试者条目 → 进入 UC-13 详情页。
- 验收标准：
  1. 数据只来自当前研究（归属约束）；
  2. 明细中被试者以匿名标识呈现，不含可识别个人信息；
  3. 列表支持基本排序（进入时间/状态），具体表格能力按 Dashboard 组件（P1 可选）引入；
  4. 列表条目可点击进入 UC-13。
- 备注：本用例与 UC-13、M7 导出共同构成"研究数据浏览区"的下钻链路：总览 → 进度列表 → 被试者详情 → 导出（研究级批量 / 被试者级，粒度在 M7 定义）。被试者状态（进行中/完成/放弃）由 M5 会话模块维护，本用例只读展示；"放弃"的判定规则（超时/关闭页面）在 M5 定义。

#### UC-13 查看单个被试者（会话信息 + 导出入口）

- 模块：M2｜参与者：Researcher
- 目标：研究者查看某位被试者的会话信息，并通过该条目导出其全部研究数据。
- 主流程：
  1. 研究者从 UC-12 进度列表点击某被试者条目；
  2. 系统显示会话信息（匿名标识、进入/完成时间、设备信息、状态、标定质量/眼动可用性记录（如启用眼动））；
  3. 研究者点击"导出该被试者数据"→ 进入 M7 导出（单被试者级导出在 M7 定义——导出为确定需求，为 v1 数据出口）。
- 验收标准：
  1. 会话信息与该被试者会话严格对应（session 关联，无跨被试者串数据）；
  2. 展示内容不含可识别个人信息；
  3. 详情页提供"导出该被试者数据"入口（实际导出能力由 M7 交付）。
- 备注：**v1 不展示作答摘要与行为数据内容**（展示需要额外的查询与字段设计，v1 的数据出口统一为导出（M7））；作答/行为明细的页面展示若未来需要，另行评估（不在 v1、不属于可视化 P1 项）。本用例为只读展示，会话信息由 M5 维护。

### M3 信息流编辑

> 模块说明：编辑器基于 craft.js 引擎 + 自研外壳（控件库面板 / 画布 / 属性面板 / 序列化适配层）。控件 = 注册表条目（renderer + propSchema）；平台差异 = styleId + CSS 变量主题。编辑产物为信息流草稿文档，发布见 M2 UC-09。
>
> **feed 形态（已与客户确认）**：研究者新建信息流时从 **8 个起始模板中选择一个**——7 个平台模板（Facebook / X / Bluesky / Truth Social / Threads / TikTok / Instagram）+ **1 个空白画布**；选定后进入编辑界面，以该模板为初始内容与平台主题（空白画布为无平台主题的自由搭建）。

#### UC-14 新建/进入信息流编辑

- 模块：M3｜参与者：Researcher
- 目标：研究者为研究创建信息流：先选择起始模板，再进入编辑器进行编辑；已建信息流则直接进入编辑。
- 主流程：
  1. 研究者从研究详情选择"编辑信息流"；
  2. **（首次/无草稿时）**系统显示模板选择页——8 个起始模板（7 个平台模板 + 空白画布），每个模板有名称与预览示意；
  3. 研究者选择模板（如"X 模板"或"空白画布"）；
  4. 系统以所选模板为初始内容创建草稿（模板 = 预置控件树 + 平台主题），进入编辑器；
  5. **（已有草稿时）**系统直接加载该研究的 feed 草稿文档（跳过模板选择）并进入编辑器。
- 备选流程：
  - 已有草稿时研究者如需更换平台模板：提供"更换模板"入口——v1 可仅支持在当前模板内编辑，更换模板为后续增强（见备注）。
- 验收标准：
  1. 模板选择页完整展示 8 个起始模板，均可选择并正确初始化（7 平台各带对应平台主题，空白画布无主题）；
  2. 选择模板后进入编辑器，画布内容为模板预置控件树；
  3. 已有草稿时进入不经过模板选择，且内容与上次保存一致（往返转换无损——适配层验证点）；
  4. 存在未保存修改时离开有提示（保存走 UC-20）。
- 备注：模板选定后"平台主题"即确定（theme-x 等 CSS 变量组）；编辑器打开/编辑的是草稿版本，已发布版本不受影响（UC-09 快照语义）。"已建信息流后更换平台模板"v1 暂不支持（属模板级重构），如需支持列入 P1 评估。

#### UC-15 向信息流添加内容

- 模块：M3｜参与者：Researcher
- 目标：研究者从控件库添加控件到信息流（在所选模板/空白画布内继续搭建）。
- 主流程：
  1. 研究者打开控件库（控件条目：如帖子卡片、头像、文本、图片、点赞/分享按钮等，均为当前平台风格——沿用模板平台主题）；
  2. 研究者通过拖拽（craft.js 内置）或点击添加将条目加入画布；
  3. 新内容出现在信息流末尾（或插入到目标位置）；
  4. 新内容以默认 props（含当前平台 styleId）渲染，可继续编辑（UC-17）。
- 备选流程：
  - 1a 控件库提供"整帖快捷模板"（如该平台风格的一张完整帖子，一次拖出多控件组合）——可选增强，范围由交互同学定。
- 验收标准：
  1. 模板内可添加该平台风格的控件，新控件外观与模板平台主题一致；
  2. 空白画布可自由添加任意平台风格控件（styleId 可逐个切换，UC-17）；
  3. 添加的每个控件实例有唯一 id（后续数据采集归因用 data-widget-id 的基础）。
- 备注：**跨平台混搭策略**：平台模板内添加的控件允许"模板内单控件切换为其他平台风格"（混搭）。

#### UC-16 调整信息流结构与顺序

- 模块：M3｜参与者：Researcher
- 目标：研究者对信息流中的内容进行选择、排序、复制与删除。
- 主流程：
  1. 研究者在画布中点击选中某个内容（显示选中态与操作栏）；
  2. 研究者**通过拖拽调整条目顺序**（craft.js 内置拖拽：拖动条目到目标位置改变其在列表中的先后）；使用复制、删除操作（删除需确认）；
  3. 画布即时反映变更。
- 备选流程：
  - 1a 点选容器内部子控件：选中子控件，操作栏作用于子控件；
  - 2a 拖拽排序的插入位置判定（拖到第几条之间）若需自研且体验成本过高：可临时提供上移/下移按钮兜底（仅作为实现期退路，不作为 v1 设计承诺，见备注）。
- 验收标准：
  1. 顺序调整（拖拽）/复制/删除正确生效且可撤销（编辑器撤销/重做可用）；
  2. 删除后不可见内容随之消失（无幽灵数据残留于文档）；
  3. 撤销/重做覆盖添加、排序、复制、删除与属性修改。
- 备注：**排序以 craft.js 内置拖拽为主交互，不设计上移/下移按钮**——拖拽添加与树内移动为 craft.js 原生能力；列表内"拖到精确位置"的插入体验细节可能在骨架阶段需少量自研，验证后再定是否需临时兜底。排序结果持久化于草稿文档。

#### UC-17 编辑控件属性

- 模块：M3｜参与者：Researcher
- 目标：研究者修改选中控件的属性（含平台风格 styleId），画布实时反映。
- 主流程：
  1. 研究者选中某控件；
  2. 属性面板按该控件的 propSchema 自动渲染表单（select/number/boolean/文本/图片选择等）；
  3. 研究者修改属性（如切换 styleId：X/IG/FB 风格；修改文案、计数、图片）；
  4. 画布中的控件即时按新属性渲染。
- 验收标准：
  1. 每种控件的可编辑属性与 propSchema 声明一致，无手写表单分支遗漏；
  2. 修改 styleId 后控件样式切换正确（消费对应平台 CSS 变量组）；
  3. 修改实时反映到画布（与参与端渲染同一 renderer——所见即所得）；
  4. 非法输入被轻量校验拦截（如数字范围）。
- 备注：属性修改为草稿级变更；图片类属性经素材选择（UC-18）。

#### UC-18 管理图片素材

- 模块：M3｜参与者：Researcher
- 目标：研究者上传并复用图片素材，供图片类控件使用。
- 主流程：
  1. 研究者在图片类控件的属性面板选择"上传图片"；
  2. 系统上传图片至素材存储并返回素材记录；
  3. 研究者为该控件选择素材（或从本研究的素材列表中选用已有素材）；
  4. 控件以所选图片渲染。
- 验收标准：
  1. 上传成功的图片可在控件中渲染；
  2. 同一研究内素材可复用（素材归属于研究，受 UC-04 归属约束）；
  3. 支持的文件类型与大小限制在实现时定（如 jpg/png/webp，≤5MB），超限给出明确提示。
- 备注：素材存储位置与接口归属后端（实现时定）；参与端加载图片的缓存与体积策略（信息流加载性能）随 M5 细化。

#### UC-19 预览信息流（暂不实现，P1 可选）

- 模块：M3｜参与者：Researcher｜状态：**v1 不做（记录在案）**
- 目标：研究者以"被试者视角"预览当前草稿的渲染效果（预期形态：以参与端渲染器渲染当前草稿，预览不可编辑，可切换编辑/预览模式）。
- 暂不考虑做的原因：
  1. **实现路径未验证**：在 craft.js 编辑器内直接渲染"参与端视图"（脱离 Editor 编辑上下文的渲染模式）是否可行尚未经技术验证，存在不确定性；
  2. v1 的风险可控：编辑器画布与参与端共用同一 widgets 渲染器（所见即所得设计），预览缺失不直接影响编辑正确性。
- v1 替代验证手段：研究者通过"发布 → 打开参与链接"核验实际参与端效果（代价为发布一次，供必要时使用）。
- 转正条件（P1）：编辑器 spike/骨架阶段验证 craft.js 渲染模式可行后，评估实现成本再定。
- 验收标准（实现时补充）：此处仅记录决策，不设验收标准。

#### UC-20 保存信息流草稿

- 模块：M3｜参与者：Researcher、System
- 目标：研究者保存当前信息流编辑内容为草稿。
- 主流程：
  1. 研究者点击"保存"（或触发自动保存，策略见备注）；
  2. 系统将编辑器文档经序列化适配层转换为纯净文档格式（WidgetInstance[]）；
  3. 系统校验文档合法性（控件类型存在、props 符合 propSchema、无悬空引用）；
  4. 系统保存为草稿版本并提示成功。
- 备选流程：
  - 3a 校验失败：指出问题位置，阻止保存；
  - 4a 保存失败（网络等）：提示失败，保留本地编辑状态不丢失。
- 验收标准：
  1. 保存后重新进入（UC-14）内容一致（无损往返）；
  2. 非法文档（未知控件类型、非法 props）无法保存；
  3. 保存过程不阻塞编辑（异步保存，保存状态有提示：已保存/保存中/未保存）。
- 备注：**自动保存策略（是否开启、间隔）实现时定**，v1 至少提供手动保存 + 离开提示（UC-14 备选）；草稿与 published 版本分离由数据模型保证（UC-09）。

### M4 问卷（题库 + 问卷）

> 模块说明：问卷体系分**两层**——
> - **题库层（研究者账号级）**：研究者维护自己的题目池。入口与"新建研究"平级（研究者端导航级功能），题目可跨研究复用。
> - **问卷层（研究级）**：研究内"编辑问卷" = 从本人题库**选择要启用的题目**（勾选加入）→ 编排题目顺序 → 配置跳转逻辑。
>
> **模式（已确认）**：问卷为**固定顺序 + 基于选项的分支跳转**；无随机抽题——启用哪些题目、顺序如何由研究者手动决定。题库是题目来源，不参与随机抽取。
>
> **无分区（已确认）**：问卷**没有区块/分区功能**（blocks 不在范围）；问卷**仅在浏览结束后提供**——不存在浏览前问卷或浏览穿插问卷（参与流程见 M5）。
>
> **锁定规则（简化开发决策）**：问卷一经发布（随研究进入使用阶段）**即不可修改**——编辑入口关闭并提示；如需变更问卷内容，须新建研究或删除重建。信息流仍支持再次发布更新快照（见 UC-09）。
>
> **题目数据归属**：题目实体存于题库（账号级）；问卷文档引用题目（记录题目 id 与启用顺序）。发布时问卷固化为快照（含题目内容快照），此后题库修改不影响已发布问卷；未发布问卷在作答前读取题库最新内容。
>
> 剩余未决项（不阻塞需求）：问卷引擎**实现方式（现成外部库 vs 自研）**未定（需技术验证后定）；题型范围默认常用题型集（单选/多选/Likert/开放文本/排序/矩阵/滑块），待客户确认后增删。

#### UC-21 管理题库

- 模块：M4｜参与者：Researcher
- 目标：研究者查看、检索与管理自己的题库（入口与"新建研究"平级）。
- 主流程：
  1. 研究者从研究者端导航进入"题库"；
  2. 系统显示题库题目列表（题型、题干、最近更新时间），支持按题型筛选/关键词检索；
  3. 研究者选择题目查看详情，或进入新增/编辑/删除（UC-22 / UC-23）。
- 验收标准：
  1. 列表只显示当前研究者的题目（UC-04 归属约束）；
  2. 题目详情完整呈现（题型、题干、选项、设置）；
  3. 列表检索可用（v1 支持简单关键词与题型筛选即可）。
- 备注：题库为研究者账号级资源，所有研究共享引用；若客户要求题库按研究隔离，调整归属即可（当前不假设）。

#### UC-22 向题库导入题目（v1：单题表单添加）

- 模块：M4｜参与者：Researcher
- 目标：研究者向题库添加题目（v1 最小实现：单题通过前端表单逐题录入）。
- 主流程：
  1. 研究者在题库页点击"新增题目"；
  2. 系统显示题目表单——先选择题型（单选/多选/Likert/开放文本/排序/矩阵/滑块，范围待客户确认），表单按题型动态呈现（题干、选项增删、是否必答等）；
  3. 研究者填写并保存；
  4. 系统校验（题干非空、选项合法等按题型规则）并入库。
- 备选流程：
  - 3a 校验失败：表单错误提示，不入库。
- 验收标准：
  1. 每种可用题型均可通过表单正确录入（题干/选项/设置完整）；
  2. 非法输入被拦截并给出明确提示；
  3. 新增题目立即出现在题库列表。
- 备注：**批量导入（粘贴结构化文本 / CSV 文件）为 P1 增强，v1 不做**——v1 仅支持单题表单录入（最小实现，见技术可行性分析）。题目录入在题库层完成，问卷层不再有"导入题目"入口。

#### UC-23 编辑 / 删除题库题目

- 模块：M4｜参与者：Researcher
- 目标：研究者修改或删除题库中的题目。
- 主流程：
  1. 研究者在题库列表选中题目；
  2. **编辑**：打开与 UC-22 相同的表单，修改后保存；
  3. **删除**：系统确认（提示该题目可能被未发布问卷引用），研究者确认后删除。
- 验收标准：
  1. 编辑保存后题库与引用方（未发布问卷）读取到最新内容；
  2. 删除需确认；删除后题目从题库消失；
  3. 已发布问卷不受题库编辑/删除影响（发布时已固化快照，锁定规则）。
- 备注：v1 简化——删除题目时若被未发布问卷引用，不做阻断（问卷端显示"题目缺失"标记并提示重新选题）；是否需要"引用检查与阻断"随实现评估。

#### UC-24 编辑问卷：从题库选择启用题目并编排

- 模块：M4｜参与者：Researcher
- 目标：研究者为研究编排问卷——从本人题库勾选启用题目并调整顺序（问卷无区块/分区，仅线性顺序）。
- 主流程：
  1. 研究者从研究详情选择"编辑问卷"；
  2. 系统显示问卷编辑区（当前已启用题目列表）与题库面板（本人题库，可按题型筛选）；
  3. 研究者勾选题目加入问卷（按勾选顺序追加到问卷末尾）；
  4. 研究者调整启用题目的顺序（线性，无区块分组），移除已加入的题目；
  5. 研究者保存（UC-26）。
- 备选流程：
  - 3a 研究已发布：问卷编辑入口关闭（锁定规则），系统提示问卷不可修改。
- 验收标准：
  1. 启用题目均来自本人题库（UC-04）；
  2. 加入/移除/顺序调整正确生效且可撤销；
  3. 未发布问卷可随时增删启用题目（引用关系，题目内容取自题库最新）；
  4. 发布后编辑入口关闭并有明确提示。
- 备注：问卷无"导入/新建题目"入口——题目一律先在题库维护（UC-21~23）再被问卷启用；问卷仅在浏览结束后呈现（无浏览前问卷/穿插，见 M4 模块说明与 M5 参与流程）。

#### UC-25 配置分支跳转逻辑

- 模块：M4｜参与者：Researcher
- 目标：研究者为问卷配置基于选项的分支跳转——作答某题的不同选项将引导被试者跳转到不同题目/分支。
- 主流程：
  1. 研究者选中某题（或某选项组），打开"跳转逻辑"编辑；
  2. 系统展示该题的选项与当前默认流向（默认：下一题）；
  3. 研究者为每个选项（或选项组）设置目标——下一题 / 跳转到指定题号 / 跳转到指定分支（跳过中间题目）；
  4. 系统校验跳转目标合法（目标为已启用题目、无死循环/不可达配置冲突）；
  5. 研究者保存（随问卷草稿，UC-26）。
- 备选流程：
  - 3a 未设置跳转的选项：默认流向下一题；
  - 4a 校验失败（如跳转到未启用/不存在的题目、形成无法到达的分支）：指出问题位置，阻止保存该规则。
- 验收标准：
  1. 每个选项可独立配置流向（下一题/跳转指定题/跳转分支）；
  2. 跳转目标非法时保存被阻止并给出定位提示；
  3. 规则持久化于问卷文档，运行时按规则路由（确定性，见 M5 问卷运行）；
  4. 跳转逻辑的编辑与验证不依赖可视化流程图（v1 表单式；React Flow 可视化仍为 P1 候选）。
- 备注：**随机化（randomizers）**为 spec 保留项——题库随机抽题已排除，但"选项顺序随机化"是否仍需要待与客户最后核对（默认保留可配置项，若客户确认不需要则裁剪）；实现方式（外部库 vs 自研引擎）未定，不影响本用例的规则语义。

#### UC-26 保存问卷

- 模块：M4｜参与者：Researcher、System
- 目标：研究者保存问卷的当前编排（启用题目、顺序、跳转规则）。
- 主流程：
  1. 研究者点击"保存"；
  2. 系统校验：启用题目均存在且来自本人题库；跳转目标均为已启用题目；无死循环/不可达冲突；
  3. 系统保存问卷草稿并提示成功。
- 备选流程：
  - 2a 校验失败：指出问题位置，阻止保存；
  - 3a 保存失败（网络等）：提示失败，保留本地状态。
- 验收标准：
  1. 保存后重新进入（UC-24）编排一致（无损往返）；
  2. 引用缺失（题目被删）或跳转非法时保存被阻止；
  3. 保存不阻塞编辑（异步，状态提示）。
- 备注：问卷草稿与信息流草稿各自独立保存；发布时问卷被固化锁定（UC-09 / M4 锁定规则）。

#### UC-27 问卷试答（暂不实现，P1 可选）

- 模块：M4｜状态：**v1 不做（记录在案）**
- 目标：研究者以被试者身份试答当前问卷，验证流程（预期形态：以参与端作答渲染器试跑问卷草稿）。
- 暂不考虑做的原因：与 M3 UC-19 预览同理——编辑器内试答路径未验证；v1 可通过"发布 → 参与链接试答"代替（研究者自行以被试者身份进入）。
- 转正条件：同 UC-19（P1，验证渲染路径后评估）。
- 验收标准（实现时补充）：此处仅记录决策，不设验收标准。

### M5 参与会话与问卷运行

> 模块说明：本模块是被试者端核心流程。**参与流程（已确认）**：进入（链接/token）→ 知情同意 →（眼动标定，若研究启用，采集实现见 M6）→ **浏览模拟信息流** → **浏览结束后提供问卷**（无浏览前问卷/穿插，问卷固定顺序 + 选项跳转）→ 提交完成。
> 会话以**匿名 token** 建立（非 JWT，不建账号）；会话状态（进行中/完成/放弃）由本模块维护，供 M2 UC-12 进度展示只读引用。行为与眼动数据采集（M6）与本模块并行运行——渲染层自动注入 data-widget-id，采集不阻塞流程。问卷内容来自 published 快照（M4 锁定规则）。

#### UC-28 进入研究并创建会话

- 模块：M5｜参与者：Participant、System
- 目标：被试者通过参与链接进入研究，系统创建匿名会话。
- 主流程：
  1. 被试者打开参与链接（含 token）；
  2. 系统校验：研究存在且状态为"收集中"；token 有效；
  3. 系统创建匿名会话（生成匿名被试者标识、记录设备信息与进入时间）；
  4. 系统进入参与流程（下一步：知情同意 UC-29）。
- 备选流程：
  - 2a 研究不存在/token 无效：显示无效链接提示；
  - 2b 研究已关闭或未发布：显示对应提示（如"本研究已结束"），不创建会话；
  - 2c 研究收集中但问卷/信息流未配置完成：提示"研究尚未就绪"（防御性检查，正常流程下发布校验已保证）。
- 验收标准：
  1. 有效链接创建会话并进入同意步骤；
  2. 关闭/无效研究不创建会话并给出明确提示；
  3. 会话匿名（无可识别个人信息，与研究者账号体系隔离）。
- 备注：同一浏览器重复打开链接是否创建新会话（一人多次参与的限制）v1 不做限制——是否限制由客户后续定（记录在案）。会话创建即产生会话元数据（进入时间、设备信息）。

#### UC-29 知情同意

- 模块：M5｜参与者：Participant、System
- 目标：被试者阅读知情同意说明并决定是否参与（同意前不采集任何行为数据）。
- 主流程：
  1. 系统展示知情同意页（研究目的、采集内容（交互行为/眼动）、隐私说明、可随时退出）；
  2. 被试者选择"同意并开始"或"不同意"；
  3a 同意：系统记录同意（含时间戳）与会话关联，进入浏览流程（UC-30）；
  3b 不同意：系统结束会话（状态：放弃），不采集任何数据。
- 验收标准：
  1. 未同意前不启动任何行为/眼动采集；
  2. 同意记录与会话关联（可追溯）；
  3. 不同意则流程终止，无数据残留。
- 备注：同意文本与隐私文档作为研究配置随发布固化（v1 使用平台默认模板，是否允许研究者自定义同意文本待客户确认）。

#### UC-30 浏览模拟信息流

- 模块：M5｜参与者：Participant、System
- 目标：被试者浏览已发布的信息流内容（行为/眼动采集并行进行，见 M6）。
- 主流程：
  1. 系统渲染 published 信息流快照（按平台模板/空白画布内容，桌面端——无移动端目标，已确认）；
  2. 被试者浏览帖子内容（滚动浏览；呈现方式随浏览结束触发方式细化，见备注）；
  3. 系统在后台持续采集交互与（启用时）眼动数据（M6，data-widget-id 归因）；
  4. 浏览阶段结束（判定方式见备注）→ 进入问卷（UC-31）。
- 验收标准：
  1. 渲染内容与发布快照一致（非草稿）；
  2. 浏览期间行为/眼动采集正常运行且不阻塞浏览（页面流畅）；
  3. 浏览结束前问卷不可见。
- 备注：**浏览结束的判定方式（决策点，待客户确认）**——两种候选：① **被试者浏览完后点击"完成浏览"按钮**进入问卷（浏览节奏由被试者控制）；② **设定浏览时长，到时系统自动跳转**进入问卷（时长由研究配置）。由客户确认采用哪一种（或两种都支持、按研究配置选择）；确认后本用例细化第 4 步。无论哪种方式，跳转前需保证本地采集数据已缓冲/上报（M6），不丢失浏览阶段数据。

#### UC-31 作答问卷（含跳转运行时）

- 模块：M5｜参与者：Participant、System
- 目标：被试者在浏览结束后完成问卷；系统按跳转规则路由题目。
- 主流程：
  1. 浏览结束后系统进入问卷（呈现 published 问卷快照中的第一题）；
  2. 被试者逐题作答（题型渲染：单选/多选/Likert/开放文本等；必答校验）；
  3. 提交当前题后，系统按该题作答值评估跳转规则：默认下一题，或按选项跳转到指定题（跳过中间题目）；
  4. 所有应答题作答完毕 → 被试者确认提交；
  5. 系统校验完整性并记录提交，会话状态置为"完成"（UC-33）。
- 备选流程：
  - 2a 必答题未答：阻止进入下一题并提示；
  - 3a 跳转目标异常（防御性）：回退默认下一题并记录异常（正常流程下发布/保存校验已保证目标合法）；
  - 4a 提交中断（网络）：数据本地保留，恢复后重试（UC-32）。
- 验收标准：
  1. 跳转规则在运行时按作答值正确路由（确定性，与 UC-25 规则一致——测试覆盖）；
  2. 每题作答与时间戳被记录并与会话关联；
  3. 提交后会话进入"完成"状态，问卷不可再作答（锁定）；
  4. 未答完的跳转跳过题目不出现作答记录（符合跳转语义）。
- 备注：问卷作答记录为数据（不展示），出口为导出（M7）；随机化保留项（选项顺序随机化）若确认保留，此处运行时按种子执行（同 M4 UC-25 备注）。

#### UC-32 会话进度保存与续传

- 模块：M5｜参与者：Participant、System
- 目标：会话在刷新/断线后可恢复，不丢失已完成的进度。
- 主流程：
  1. 系统在阶段边界保存会话进度（同意完成、浏览完成、问卷逐题作答）；
  2. 被试者刷新页面或网络恢复后重新打开链接；
  3. 系统识别未完成会话，恢复到对应阶段继续；
  4. 恢复后流程从断点继续（浏览中中断 → 重新进入浏览阶段；问卷中中断 → 回到当前题）。
- 备选流程：
  - 3a 会话已超时/被判定放弃（UC-33）：提示会话已结束，不恢复。
- 验收标准：
  1. 刷新/断线不丢失已保存进度（已答题目不重复作答）；
  2. 恢复后行为数据的时间线仍连续可关联（同一会话）；
  3. 已"完成/放弃"的会话不可恢复作答。
- 备注：**恢复粒度决策**：v1 为"阶段 + 问卷当前题"粒度；浏览阶段内部的中断恢复策略（重新开始浏览 vs 从记录点继续）随 UC-30 的呈现方式确定后细化；断网期间的采集数据由 M6 IndexedDB 缓冲补传。

#### UC-33 会话结束与状态判定（完成/放弃/超时）

- 模块：M5｜参与者：System
- 目标：系统正确判定会话终态并维护状态，供进度展示（M2 UC-12）使用。
- 主流程：
  1. 正常完成：被试者提交问卷（UC-31）→ 状态"完成"，显示结束页（感谢/提示可关闭）；
  2. 主动放弃：被试者在任意阶段关闭页面/退出 → 系统尽力标记"放弃"（页面卸载上报为尽力而为）；
  3. 超时放弃：超过无活动时限（如 30 分钟，实现时定）→ 会话标记"放弃"；
  4. 研究关闭：研究被研究者关闭（UC-10）→ 进行中会话标记为"放弃"（不可继续提交）。
- 验收标准：
  1. 完成/放弃的会话状态正确且不可再进入作答（与 UC-28 校验联动）；
  2. 超时判定有明确规则（无活动时限）且记录最后活动时间；
  3. 状态变化与 M2 UC-12 进度列表一致（进行中/完成/放弃计数正确）。
- 备注：超时时长的判定阈值实现时定；页面关闭瞬间的数据上报为尽力而为（断网场景由 M6 缓冲补传兜底）。

### M6 行为采集与眼动

> 模块说明：本模块为**系统侧能力**，运行于参与端（目标平台：桌面浏览器）。
> **启动前提**：仅在知情同意后启动采集（M5 UC-29）；研究未启用眼动时不采集眼动数据。
> **原始数据原则（已确认）**：系统**只采集与存储原始事件数据**，不做任何衍生数据处理——停留时长、打字速度、热力图等衍生计算**均不在范围**（研究者从原始数据自行分析，见 M7 导出）。
> **采集范围**：行为数据项以客户确认为准（[D-5]，默认清单：点击/鼠标坐标/键盘/滚动/页面可见性变化；**无触摸事件**——已确认无移动端目标）。
> **架构约束**：采集与渲染解耦（事件委托 + data-widget-id 归因，data-widget-id 由渲染层注入）；事件存储为"事件类型 + JSONB 负载"（见技术架构）；行为事件节流采样（鼠标 10–20Hz）；**眼动采样频率为每帧**（每个渲染/注视估计帧采集一次）；眼动采样优先 Web Worker（实现受阻时主线程采样为技术回退，产出数据一致）。
> **眼动与行为相互独立**：眼动不可用（无摄像头/权限被拒/浏览器不支持）时**仅跳过眼动采集并记录原因，不阻塞浏览、问卷等任何其他环节**；不存在"降级为行为日志模式"的概念（行为采集本就独立运行）。
> **隐私边界**：摄像头原始视频**永不传输/存储**（仅浏览器内处理）；采集仅在会话内有效，会话结束停止。

#### UC-34 行为数据采集与可靠上报（System）

- 模块：M6｜参与者：System
- 目标：会话期间持续采集交互行为数据并可靠上报入库，不丢失、不阻塞浏览。
- 主流程：
  1. 会话进入浏览/作答阶段后，系统启动采集（同意后）；
  2. 监听原生事件（点击/鼠标移动/键盘/滚动/可见性变化），按事件类型节流采样（鼠标 10–20Hz 等）；
  3. 每条事件记录：事件类型、时间戳、坐标/负载、目标控件 data-widget-id、会话标识；
  4. 事件写入本地缓冲（IndexedDB）；
  5. 缓冲数据批量上报后端（断线自动重试，恢复后补传）；
  6. 后端校验并入库（行为事件表：类型 + JSONB 负载，按会话/控件索引）。
- 备选流程：
  - 5a 上报失败（网络）：保留于本地缓冲，下次上报窗口重试；
  - 5b 会话结束仍有未上报数据：会话结束前尽力上报（best-effort），失败数据保留并在下次同会话恢复时补传（v1 语义见备注）。
- 验收标准：
  1. 采集数据项与确认清单一致；
  2. 采样频率符合设定（鼠标 10–20Hz），页面保持流畅（与 M5 UC-30 验收一致）；
  3. 事件含会话与控件归因，可与作答/会话关联（无跨会话串数据）；
  4. 断网/刷新期间数据不丢失（缓冲 + 重试 + 补传）；
  5. 采集不影响问卷跳转与流程推进（非阻塞）。
- 备注：事件时间戳采用统一时钟源（performance/服务器时间对齐策略实现时定）；采集仅在同意后启动（启动点由 M5 流程控制）；**仅记录原始事件（含时间戳与归因），不做衍生计算（已确认，见模块说明）**。

#### UC-35 眼动标定（研究启用眼动时）

- 模块：M6｜参与者：Participant、System
- 目标：眼动研究在浏览前尝试标定以建立映射基准并评估质量；**标定未通过不阻止眼动追踪**——追踪照常进行，质量被记录。
- 主流程：
  1. 研究启用眼动且被试者同意后，系统进入标定页（界面由交互同学实现，逻辑由 AI 同学实现）；
  2. 系统提示被试者注视屏幕上的标定点并点击（9 点标定）；
  3. 系统计算标定质量评分并记录；
  4. **无论质量是否达标，系统均进入浏览阶段（M5 UC-30）并继续眼动采集**——标定质量仅作为数据质量标识随眼动数据记录（供导出过滤），不作为是否采集的开关。
- 备选流程：
  - 2a 摄像头不可用/权限被拒：**跳过标定与眼动采集，记录原因（眼动不可用），直接进入浏览阶段**——不阻塞任何其他测试环节；
  - 2b 标定质量过低：提示被试者重试（限次，如 2 次，帮助提高数据质量），被试者可选择继续——无论结果如何均可进入浏览。
- 验收标准：
  1. 标定流程可完成且质量评分被记录（与会话关联）；
  2. **标定未通过/放弃标定后，眼动追踪仍继续运行**（事件带低质量标识）；
  3. 无摄像头/权限被拒时流程不被阻塞（跳过眼动直接进入浏览与问卷）；
  4. 标定过程本身不产生注视采集数据（仅建立基准）。
- 备注：标定 UI 的交互细节（提示语、重试次数）由交互同学提供；标定质量与眼动可用性记录供导出时数据质量过滤（M7 语义，v1 不展示）。

#### UC-36 眼动数据采集与控件归因（System）

- 模块：M6｜参与者：System
- 目标：浏览期间**每帧**采集注视数据并归因到具体控件（不依赖标定是否通过）。
- 主流程：
  1. 浏览阶段启动眼动采集（**每帧采样**——每个渲染/注视估计帧采集一次；优先 Web Worker，实现受阻时主线程采样为技术回退，产出数据一致）；
  2. 系统将 gaze 坐标映射到页面元素：elementFromPoint → closest('[data-widget-id]')；
  3. 记录眼动事件：**注视点像素坐标 (x, y)**（视口坐标系）、命中控件 data-widget-id、时间戳、会话标识、标定质量标识——**坐标与控件归因同时记录，坐标不被归因取代**（归因仅为附加标注）；
  4. 与行为事件同一缓冲/上报/入库链路（gaze_events）。
- 备选流程：
  - 2a 命中非内容区域（空白/导航）：记录为"未命中内容"，不丢弃。
- 验收标准：
  1. 眼动事件按每帧采样产生且归因正确（命中控件与页面实际布局一致——抽样验证）；
  2. **每条眼动事件均含原始 (x, y) 像素坐标**；未命中内容（归因失败）时坐标仍被完整记录，不因归因失败而丢弃事件；
  3. 原始摄像头视频不离开浏览器（无视频上传网络请求）；
  4. 眼动数据与行为数据同一时间轴、同一会话关联（无跨会话串数据）；
  5. 采集不阻塞浏览（页面流畅）。
- 备注：注视点坐标与命中控件同时保留（坐标供研究者复算，归因供快捷分析）；隐私约束（原始视频不传输）为硬性实现约束，列入验收与代码审查项。

#### UC-37 眼动可用性与质量记录（System）

- 模块：M6｜参与者：System
- 目标：眼动不可用（无摄像头/权限被拒/浏览器不支持）时跳过眼动采集而不影响任何其他环节，并记录会话的眼动可用性与质量信息。
- 主流程：
  1. 系统检测眼动可用性：浏览器摄像头支持 + 权限授予（检测失败即不可用）；
  2a 可用：按 UC-35/UC-36 执行标定与每帧采集；
  2b 不可用：**跳过眼动采集**（标定与注视采集均不执行），记录原因到会话；
  3. 无论可用与否，浏览与问卷等其余流程正常进行（不阻塞、不中断）；
  4. 会话结束将可用性/质量记录（是否采集眼动、原因、标定质量）与数据一并持久化（供导出过滤，v1 不展示）。
- 验收标准：
  1. 眼动不可用时流程不受任何阻塞（浏览、问卷、提交全部正常）；
  2. 会话质量记录完整（眼动是否采集、不可用原因、标定质量），导出时可区分；
  3. 眼动不可用的会话不产生任何眼动事件（无错误/伪造数据）。
- 备注：**无"降级为行为日志"概念**（已确认）——行为采集独立运行，眼动不可用只影响眼动数据本身；标定失败不构成"不可用"（照常采集，质量偏低，见 UC-35）。目标平台为桌面端，无移动端适配（已确认）。

### M7 数据导出

> 模块说明：导出为 **v1 确定需求**，是研究者获取研究数据的**唯一出口**（范围注记：v1 不展示任何研究数据内容）。
> **内容为原始数据**（已确认原则）：导出问卷作答、行为事件、眼动事件与会话元数据的原始记录，**不包含任何衍生指标**；研究者基于导出的原始 CSV 自行分析。
> **导出粒度**：研究级批量（UC-38）与单被试者级（UC-39，入口在 UC-13）。
> **关键机制**：大数据量流式生成 + **异步任务**（不阻塞请求，任务状态可见）；**完整性校验**（会话完整性、时间戳一致、归因无串会话）；会话质量字段（眼动可用性/标定质量）随数据导出，供研究者过滤；时间戳采用统一格式。
> 权限：导出为研究者受保护操作（UC-04 归属约束——只能导出自己研究的数据）。

#### UC-38 导出研究数据（研究级批量）

- 模块：M7｜参与者：Researcher、System
- 目标：研究者导出某研究在指定范围内的全部原始数据（问卷作答 + 行为事件 + 眼动事件 + 会话元数据）。
- 主流程：
  1. 研究者从研究详情（或参与情况页）选择"导出数据"；
  2. 系统展示导出选项：数据范围（默认：全部已完成/进行中会话；v1 可先支持"全部会话"，按状态筛选为后续增强）、数据类别（问卷作答 / 行为事件 / 眼动事件，默认全选）；
  3. 研究者确认发起导出；
  4. 系统创建异步导出任务并返回任务状态（进行中）；
  5. 系统按会话流式读取原始记录，生成分文件 CSV（sessions / responses / behavior_events / gaze_events），执行完整性校验（各会话记录完整、时间戳时序一致、无跨会话串数据）；
  6. 校验通过后打包为 zip，任务状态置为"完成"，提供下载链接；
  7. 研究者下载 zip。
- 备选流程：
  - 4a 导出失败（数据异常/校验不过）：任务状态置为"失败"，提示原因（定位到异常会话/记录），可重新发起；
  - 6a 数据量极大：生成时间较长，任务状态页持续显示进度，研究者可离开稍后回来下载。
- 验收标准：
  1. zip 内各 CSV 与数据库原始记录一致；无衍生指标混入；
  2. 完整性校验能发现并拒绝导出异常数据（如记录缺失的会话）；
  3. 会话质量字段（眼动是否采集/标定质量/不可用原因）出现在会话文件或随行字段中，研究者可按质量过滤；
  4. 异步导出不阻塞其他操作；任务状态正确流转（进行中→完成/失败）；
  5. 只能导出归属自己的研究（UC-04）。
- 备注：CSV 文件结构（列定义）与编码（UTF-8）在实现时定义并随导出附 schema 说明（README）；眼动事件含原始 (x,y) 坐标列（见 UC-36）；"随机化种子"类字段仅在随机化保留项确认需要时导出（见 UC-25 备注，当前问卷为固定顺序无随机抽题）。

#### UC-39 导出单个被试者数据（单会话级）

- 模块：M7｜参与者：Researcher、System
- 目标：研究者从 UC-13 被试者详情页导出该被试者本次会话的全部原始数据。
- 主流程：
  1. 研究者进入某被试者详情页（UC-13）点击"导出该被试者数据"；
  2. 系统创建该会话的异步导出任务；
  3. 系统导出该会话的作答记录、行为事件、眼动事件与会话信息（含质量字段），CSV 分文件 + 完整性校验；
  4. 任务完成后研究者下载。
- 备选流程：
  - 3a 该会话无某类数据（如未采集眼动）：对应 CSV 文件仍生成（含表头）或省略并在说明中注明——实现时定，推荐省略并在 zip 内 README 注明，避免研究者误读为空数据。
- 验收标准：
  1. 导出数据严格对应该会话（无跨会话串数据）；
  2. 内容与 UC-38 中该会话的记录一致；
  3. 归属约束（UC-04）。
- 备注：本用例即 UC-13 的"数据出口"，与 UC-38 共享同一导出管道与校验逻辑。

#### UC-40 导出任务管理（查看状态与下载）

- 模块：M7｜参与者：Researcher、System
- 目标：研究者查看导出任务状态并在完成后下载结果。
- 主流程：
  1. 研究者打开"导出任务"列表（或发起导出后的状态页）；
  2. 系统显示任务状态（进行中/完成/失败）与发起时间；
  3a 完成：研究者下载 zip（下载链接有效期策略实现时定，如 24 小时）；
  3b 失败：查看失败原因，修正后重新发起（UC-38/39）；
  4. （可选）研究者删除/清理历史导出任务（保留策略实现时定）。
- 验收标准：
  1. 任务状态与实际执行一致；
  2. 完成任务的下载文件可正常获取；
  3. 失败任务给出可理解的失败原因。
- 备注：历史任务保留策略（数量/时长）实现时定；v1 至少支持"当前任务状态可见 + 完成后下载"。

## 4. 功能需求（FR）

> 由用例推导；每条 FR 追溯来源用例。编号全局顺序，按模块分组追加。
>
> 推导粒度说明：一个用例可拆出多条 FR，拆分原则为"**可独立开发、可独立验收**"；v1 不做的功能仍保留 FR 条目并标注"v1 不做"（决策记录见对应用例）。

### M1 账号与权限

| 用例 | 对应 FR |
|---|---|
| UC-01 研究者注册 | FR-01 注册表单与账号创建 |
| UC-01 研究者注册 | FR-02 注册冲突处理 |
| UC-02 研究者登录 | FR-03 登录认证 |
| UC-02 研究者登录 | FR-04 JWT 签发与会话建立 |
| UC-03 研究者登出 | FR-05 研究者登出 |
| UC-04 访问受保护功能（权限控制，横切） | FR-06 请求认证（横切） |
| UC-04 访问受保护功能（权限控制，横切） | FR-07 角色授权（横切） |
| UC-04 访问受保护功能（权限控制，横切） | FR-08 资源归属校验（横切） |
| UC-05 研究者修改密码 | FR-09 研究者修改密码 |
| UC-06 忘记密码/自助重置（v1 不做） | FR-10 忘记密码/自助重置（v1 不做） |

#### FR-01 注册表单与账号创建

- 需求：提供注册表单（邮箱、密码、确认密码）；提交后校验邮箱格式、两次密码一致、密码强度（默认 ≥8 位）；通过后创建账号（默认角色 researcher）。
- 来源用例：UC-01 主流程。
- 验收标准：
  1. 注册页包含邮箱、密码、确认密码三个输入项；
  2. 邮箱格式非法时不提交并提示；
  3. 两次密码不一致时不提交并提示；
  4. 合法提交后账号创建成功，界面提示可登录（或自动登录）；
  5. 新建账号可用 FR-03 登录成功，且角色为 researcher（经 FR-07 验证）。

#### FR-02 注册冲突处理

- 需求：邮箱已被注册时提示"该邮箱已注册"并引导登录；密码不一致/强度不足时表单错误提示且不创建账号（提示不泄露账号细节）。
- 来源用例：UC-01 备选 4a/4b。
- 验收标准：
  1. 已注册邮箱再次注册 → 提示"该邮箱已注册"，不创建重复账号；
  2. 密码强度不足（<8 位等）→ 表单错误提示，不创建账号；
  3. 冲突提示不含账号其他信息（不可据此枚举账号状态细节）。

#### FR-03 登录认证

- 需求：凭邮箱 + 密码登录；校验失败返回统一错误提示（不区分"账号不存在/密码错误"，防枚举）。
- 来源用例：UC-02 主流程 4 + 备选 3a。
- 验收标准：
  1. 正确凭据 → 登录成功并进入 dashboard；
  2. 密码错误与邮箱不存在 → 返回完全相同的错误提示文案；
  3. 登录失败停留在登录页，无业务数据返回。

#### FR-04 JWT 签发与会话建立

- 需求：认证成功后签发 JWT（含 userId、role、过期时间），前端保存并在后续请求携带（Authorization header）。
- 来源用例：UC-02 主流程 5–6。
- 验收标准：
  1. 登录成功响应返回 JWT，解码载荷含 userId、role、过期时间（exp）三要素；
  2. 前端将令牌用于后续请求的 Authorization header（实测携带后访问受保护接口可通过）；
  3. 令牌过期时间与 NFR-02 设定一致。

#### FR-05 研究者登出

- 需求：清除前端保存的 JWT 并跳转登录页；登出后访问受保护页面被拒绝（401）。
- 来源用例：UC-03。
- 验收标准：
  1. 点击登出后本地令牌被清除（存储中不再存在）；
  2. 登出后访问受保护页面被重定向到登录页（请求返回 401）；
  3. 登出不依赖服务端会话（无状态语义，重复点击无副作用）。

#### FR-06 请求认证（横切）

- 需求：受保护端点校验请求 JWT 的签名与有效期；无/无效/过期令牌一律返回 401，不进入业务逻辑。
- 来源用例：UC-04 备选 2a｜备注：横切——后续模块 FR 继承。
- 验收标准：
  1. 不带令牌请求任一受保护端点 → 401；
  2. 篡改（签名无效）令牌 → 401；
  3. 过期令牌 → 401；
  4. 401 响应不包含任何业务数据。

#### FR-07 角色授权（横切）

- 需求：通过认证的请求校验角色为 researcher（URL/方法级 RBAC 规则）；角色不符返回 403。
- 来源用例：UC-04 备选 3a｜备注：横切——后续模块 FR 继承。
- 验收标准：
  1. 持非 researcher 角色令牌调用研究者专属端点（如发布/导出）→ 403；
  2. 角色判定基于服务端校验的令牌载荷（不信任前端声明）；
  3. 403 响应不含业务数据。

#### FR-08 资源归属校验（横切）

- 需求：受保护资源（研究/题库等）校验归属当前研究者；他人资源拒绝访问（403/404 采用隐身策略防探测，实现时定）。
- 来源用例：UC-04 备选 4a｜备注：横切——后续模块 FR 继承。
- 验收标准：
  1. 研究者 A 读/写研究者 B 的资源均被拒绝；
  2. 若采用 404 隐身策略：A 请求 B 的资源与请求不存在的资源，响应完全一致（同为 404 且无差异信息）；
  3. 归属校验覆盖后续所有资源型端点（各模块集成测试抽查）。

#### FR-09 研究者修改密码

- 需求：研究者登录后凭当前密码修改密码；当前密码错误则拒绝；新密码满足强度（与注册一致）；成功后旧密码立即失效。
- 来源用例：UC-05。
- 验收标准：
  1. 当前密码输入错误 → 拒绝修改并提示；
  2. 新密码强度不足或两次输入不一致 → 拒绝并提示；
  3. 修改成功后，用旧密码调用登录（FR-03）失败；
  4. 修改成功后，用新密码登录成功。

#### FR-10 忘记密码 / 自助重置（v1 不做）

- 需求：研究者忘记密码时通过邮箱重置链接自助重置（标准形态）。
- 状态：**v1 不做**——原因与转正条件见 UC-06 决策记录；保留本条以便范围讨论与追溯。
- 来源用例：UC-06。
- 验收标准：v1 不适用（不实现即不验收）；若转正，按标准形态补充：① 申请重置后邮箱收到一次性有效链接；② 链接使用一次后失效；③ 重置成功后旧密码立即失效。

### M2 研究管理

> M2 所有研究者操作继承横切 FR-06/07/08（认证/角色/归属），以下不再逐条重复。

| 用例 | 对应 FR |
|---|---|
| UC-07 创建研究 | FR-11 创建研究 |
| UC-08 查看与编辑研究 | FR-12 查看研究列表与详情 |
| UC-08 查看与编辑研究 | FR-13 编辑基本信息与运行设置 |
| UC-09 发布研究 | FR-14 发布研究 |
| UC-09 发布研究 | FR-15 重新发布（更新信息流） |
| UC-10 关闭/重新开放研究 | FR-16 关闭研究 |
| UC-10 关闭/重新开放研究 | FR-17 重新开放研究 |
| UC-11 删除研究 | FR-18 删除研究 |
| UC-12 查看参与总览与进度列表 | FR-19 参与总览与进度列表 |
| UC-13 查看单个被试者 | FR-20 单个被试者会话信息 |
| UC-13 查看单个被试者 | FR-21 单被试者数据导出入口 |

#### FR-11 创建研究

- 需求：研究者创建研究（标题、描述），初始状态"草稿"，归属当前研究者。
- 来源用例：UC-07。
- 验收标准：
  1. 创建成功后研究出现在"我的研究"列表，状态为草稿；
  2. 标题/描述保存正确，可进入研究工作区；
  3. 新研究归属当前研究者（他人列表不可见，继承 FR-08）。

#### FR-12 查看研究列表与详情

- 需求：研究者查看自己名下的研究（标题、状态、创建时间）与研究详情。
- 来源用例：UC-08。
- 验收标准：
  1. 列表只含当前研究者的研究（含状态字段）；
  2. 打开详情可查看基本信息与（若已发布）参与链接；
  3. 列表为空时给出空态提示。

#### FR-13 编辑研究基本信息与运行设置

- 需求：研究者修改研究标题/描述与运行设置；草稿可自由修改；已发布（收集中）状态下修改不影响已发布快照。
- 来源用例：UC-08 主流程 + 备选 3a。
- 验收标准：
  1. 草稿状态：修改保存后列表/详情立即反映；
  2. 收集中状态：基本信息可改且已发布快照内容不变（对照 UC-09 发布内容验证）；
  3. 收集中状态：问卷编辑入口关闭并有明确提示（M4 锁定规则）；
  4. 运行设置修改仅对新会话生效（若含眼动开关等，旧会话不受影响）。

#### FR-14 发布研究

- 需求：研究者发布草稿研究——校验内容完整（信息流非空、问卷已配置）→ 固化快照（问卷锁定）→ 状态"收集中"→ 生成参与链接。
- 来源用例：UC-09 主流程。
- 验收标准：
  1. 信息流或问卷未配置时发布被阻止，提示缺失项；
  2. 发布成功后研究状态为"收集中"，返回可访问的参与链接；
  3. 参与者经链接访问到的是 published 快照（修改草稿不影响已访问内容）；
  4. 发布后问卷锁定（编辑入口关闭，见 FR-13 验收 3）；
  5. 发布操作原子完成（无"半发布"状态，配合 NFR-08）。

#### FR-15 重新发布（更新信息流）

- 需求：研究收集中时再次发布——仅更新信息流为新快照；问卷保持首次发布版本不可变更。
- 来源用例：UC-09 备选 4a。
- 验收标准：
  1. 再次发布后，新会话参与者看到更新后的信息流内容；
  2. 问卷内容与首次发布一致（未被再次发布改变）；
  3. 已采集数据仍关联各自版本（可区分新旧会话访问的版本——数据模型支持）。

#### FR-16 关闭研究

- 需求：研究者关闭研究——状态"已关闭"、参与链接失效、新会话不可创建；进行中会话不可提交。
- 来源用例：UC-10。
- 验收标准：
  1. 关闭后原链接访问提示"本研究已结束"，不创建新会话；
  2. 关闭前创建的进行中会话不可继续提交（作答入口关闭并提示）；
  3. 已采集数据不受关闭影响（进度列表仍可查看，导出不受限）。

#### FR-17 重新开放研究

- 需求：研究者将已关闭研究重新开放——状态回"收集中"，链接恢复可用。
- 来源用例：UC-10 主流程 4。
- 验收标准：
  1. 重新开放后原链接可再次创建新会话；
  2. 重新开放不改变已发布内容与已采集数据。

#### FR-18 删除研究

- 需求：研究者删除研究——二次确认后级联删除研究及其全部关联数据。
- 来源用例：UC-11。
- 验收标准：
  1. 删除前必须二次确认，取消则不删除；
  2. 确认删除后研究从列表消失，关联数据（快照/会话/作答/事件）不可再访问（NFR-07 保证无孤儿）；
  3. 非归属研究者无法删除（继承 FR-08）；
  4. 删除研究不影响研究者题库（题库为账号级，见 M4）。

#### FR-19 查看参与总览与进度列表

- 需求：研究者查看某研究的参与概况（总数/完成/进行中/放弃）与匿名进度列表，并可点击进入被试者详情。
- 来源用例：UC-12。
- 验收标准：
  1. 概况数字与库中会话状态统计一致（抽查比对）；
  2. 列表条目为匿名标识（无邮箱/姓名等可识别信息）；
  3. 列表支持按进入时间/状态排序（v1 基础排序）；
  4. 点击条目进入 FR-20 详情页。

#### FR-20 查看单个被试者会话信息

- 需求：研究者查看某被试者本次会话的信息（匿名标识、进入/完成时间、设备、状态、眼动可用性与标定质量）。
- 来源用例：UC-13 主流程 2。
- 验收标准：
  1. 展示信息与该会话严格对应（无跨会话串数据）；
  2. 内容匿名（不含可识别个人信息）；
  3. 会话信息字段完整呈现（含眼动可用性/标定质量记录，若研究启用眼动）。

#### FR-21 单被试者数据导出入口

- 需求：被试者详情页提供"导出该被试者数据"入口，触发 M7 单会话导出任务。
- 来源用例：UC-13 主流程 3。
- 验收标准：
  1. 入口可见且点击后进入 M7 导出流程（UC-39）；
  2. 未实现导出能力前入口禁用并提示"导出功能开发中"或隐藏——实现时定（以 M7 交付为准）。

### M3 信息流编辑

> M3 研究者操作继承横切 FR-06/07/08。编辑器外壳基于 craft.js（技术实现见技术定案）；控件/模板由 widgets 注册表与 8 模板（7 平台 + 空白）提供（已与客户确认）。

| 用例 | 对应 FR |
|---|---|
| UC-14 新建/进入信息流编辑 | FR-22 选择起始模板 |
| UC-14 新建/进入信息流编辑 | FR-23 加载已有草稿 |
| UC-14 新建/进入信息流编辑 | FR-24 未保存修改离开提示 |
| UC-15 向信息流添加内容 | FR-25 从控件库添加控件 |
| UC-15 向信息流添加内容 | FR-26 整帖快捷模板添加（可选，v1 默认不做） |
| UC-16 调整信息流结构与顺序 | FR-27 调整结构与顺序 |
| UC-17 编辑控件属性 | FR-28 编辑控件属性 |
| UC-18 管理图片素材 | FR-29 图片素材上传与使用 |
| UC-19 预览信息流（v1 不做） | FR-31 预览信息流（v1 不做） |
| UC-20 保存信息流草稿 | FR-30 保存信息流草稿 |

#### FR-22 选择起始模板

- 需求：首次编辑信息流时，研究者从 8 个起始模板（7 平台模板 + 空白画布）中选择一个；选定后以模板为初始内容创建草稿并进入编辑器（平台模板带对应平台主题，空白画布无主题）。
- 来源用例：UC-14 主流程 2–4。
- 验收标准：
  1. 模板选择页完整展示 8 个模板（名称 + 预览示意），均可选择；
  2. 选择平台模板后画布初始化为该模板预置控件树，样式应用对应平台主题（theme-x 等）；
  3. 选择空白画布后为空画布，无平台主题绑定；
  4. 选择后进入编辑器可继续编辑（配合 FR-25/28）。

#### FR-23 加载已有草稿

- 需求：已有信息流草稿的研究，进入编辑时跳过模板选择、直接加载草稿；加载内容与上次保存一致（序列化往返无损）。
- 来源用例：UC-14 主流程 5。
- 验收标准：
  1. 已有草稿时进入不显示模板选择页；
  2. 控件列表、属性、顺序与上次保存完全一致（NFR-10 自动化验证）。

#### FR-24 未保存修改离开提示

- 需求：存在未保存修改时离开编辑器，系统提示保存/放弃/取消。
- 来源用例：UC-14 备选。
- 验收标准：
  1. 无修改时离开不提示；
  2. 有修改时离开弹出提示，三种选择行为正确（保存→UC-20 流程；放弃→不保存离开；取消→留在编辑器）；
  3. 保存成功/放弃后再次进入，内容符合对应选择。

#### FR-25 从控件库添加控件

- 需求：研究者从控件库添加控件到信息流（拖拽或点击添加）；新控件以默认 props 渲染、实例唯一 id；模板模式下新控件为模板平台风格，空白画布下风格自由。
- 来源用例：UC-15 主流程。
- 验收标准：
  1. 控件库条目可拖拽或点击加入画布（两种方式均可用）；
  2. 模板模式下添加的控件外观与模板平台主题一致；空白画布下可添加任意平台风格控件（styleId 可切换，见 FR-28）；
  3. 每个新实例有唯一 id，无重复；
  4. 添加操作不触发自动保存（配合 FR-24 提示）。

#### FR-26 整帖快捷模板添加（可选增强）

- 需求：控件库提供"整帖快捷模板"（一张完整帖子的多控件组合，一次拖出）。
- 状态：**可选增强（未确认范围，v1 默认不做）**——是否提供及覆盖范围由交互同学按模板流程确认后定。
- 来源用例：UC-15 备选 1a。
- 验收标准（若实现）：整帖快捷模板一次添加生成完整控件组合（与手工逐个添加结果等价）。

#### FR-27 调整信息流结构与顺序

- 需求：研究者通过拖拽调整条目顺序（craft.js 内置）、复制与删除内容；撤销/重做覆盖添加/排序/复制/删除/属性修改；选中容器内子控件时操作作用于子控件。
- 来源用例：UC-16。
- 验收标准：
  1. 拖拽可将条目移动到目标位置（列表内前后顺序正确改变）；
  2. 复制生成独立副本（修改副本不影响原件）；
  3. 删除需确认，删除后无幽灵数据残留；
  4. 撤销（Ctrl+Z）/重做覆盖全部编辑操作；
  5. 点选容器内部子控件时，操作栏作用于子控件（层级正确）。

#### FR-28 编辑控件属性

- 需求：属性面板按控件 propSchema 自动渲染表单；研究者修改属性（含 styleId 平台风格切换）后画布实时反映；非法输入被轻量校验拦截。
- 来源用例：UC-17。
- 验收标准：
  1. 每类控件的可编辑属性与 propSchema 声明一致（新增控件无需改面板代码）；
  2. 修改 styleId 后控件样式即时切换为对应平台主题（模板模式下允许单控件切换平台风格——混搭已确认允许）；
  3. 属性修改实时反映（与参与端同一 renderer 渲染）；
  4. 非法输入（数字范围外、空必填等）被拦截并提示，不写入文档。

#### FR-29 图片素材上传与使用

- 需求：研究者上传图片素材（jpg/png/webp，≤5MB），素材归属研究并可在该研究内复用。
- 来源用例：UC-18。
- 验收标准：
  1. 上传成功的图片可在图片类控件中渲染；
  2. 同一研究内的多个控件可复用同一素材；
  3. 非法类型/超限文件被拒绝并给出明确提示；
  4. 素材归属校验（他人研究的素材不可引用，继承 FR-08）。

#### FR-30 保存信息流草稿

- 需求：保存时经序列化适配层转换为纯净文档格式并做合法性校验（未知控件类型、非法 props 阻止保存）；异步保存带状态提示；失败保留本地编辑。
- 来源用例：UC-20。
- 验收标准：
  1. 保存成功后重新进入（FR-23）内容一致；
  2. 构造非法文档（未知类型/非法 props）无法保存并被定位提示；
  3. 保存期间可继续编辑（非阻塞），状态提示正确流转（未保存/保存中/已保存）；
  4. 保存失败（断网等）提示且本地编辑不丢失。

#### FR-31 预览信息流（v1 不做）

- 需求：研究者以被试者视角预览当前草稿（参与端渲染器渲染、不可编辑）。
- 状态：**v1 不做**——原因与转正条件见 UC-19；保留本条供范围追溯。
- 来源用例：UC-19。
- 验收标准：v1 不适用；转正后补充（渲染与参与端一致、预览无编辑控件、桌面宽度渲染）。

### M4 问卷

> M4 研究者操作继承横切 FR-06/07/08。结构：题库层（账号级）+ 问卷层（研究级）；问卷无分区、仅在浏览结束后呈现；问卷发布后锁定（见 M4 模块说明）；题型范围默认常用集（待客户确认）。

| 用例 | 对应 FR |
|---|---|
| UC-21 管理题库 | FR-32 题库列表与详情 |
| UC-22 向题库导入题目 | FR-33 向题库新增题目（单题表单，v1） |
| UC-23 编辑/删除题库题目 | FR-34 编辑题库题目 |
| UC-23 编辑/删除题库题目 | FR-35 删除题库题目 |
| UC-24 编辑问卷：从题库选择启用题目并编排 | FR-36 从题库选择启用题目 |
| UC-24 编辑问卷：从题库选择启用题目并编排 | FR-37 调整启用题目顺序与移除 |
| UC-25 配置分支跳转逻辑 | FR-38 配置分支跳转逻辑 |
| UC-26 保存问卷 | FR-39 保存问卷 |
| UC-27 问卷试答（v1 不做） | FR-40 问卷试答（v1 不做） |

#### FR-32 题库列表与详情

- 需求：研究者从账号级导航进入"题库"，查看本人题目列表（题型、题干、更新时间），支持题型筛选/关键词检索与详情查看。
- 来源用例：UC-21。
- 验收标准：
  1. 题库入口与"新建研究"同级可见，仅登录研究者可访问；
  2. 列表只含本人题目（继承 FR-08），按更新时间排序；
  3. 题型筛选与关键词检索可用（v1 基础实现）；
  4. 点击题目可查看完整详情（题型、题干、选项、设置）。

#### FR-33 向题库新增题目（单题表单，v1）

- 需求：研究者通过前端表单逐题新增题目：先选择题型（默认常用集），表单按题型动态呈现（题干、选项增删、必答等），校验后入库。
- 来源用例：UC-22。
- 验收标准：
  1. 每种可用题型均可通过表单正确录入（题干/选项/设置完整）；
  2. 题干为空、选项非法等按题型规则拦截并提示；
  3. 保存成功后新题出现在题库列表；
  4. 批量导入不在 v1（无批量入口）。

#### FR-34 编辑题库题目

- 需求：研究者修改题库中题目的题干/选项/设置并保存；未发布问卷引用方读取到最新内容。
- 来源用例：UC-23 编辑。
- 验收标准：
  1. 修改保存后题库列表与详情更新；
  2. 引用该题的未发布问卷（编辑问卷时）显示最新内容；
  3. 已发布问卷不受影响（快照固化，见 NFR-14）。

#### FR-35 删除题库题目

- 需求：研究者删除题库题目（二次确认）；删除后题目从题库消失；未发布问卷引用处标记缺失并提示重新选题。
- 来源用例：UC-23 删除。
- 验收标准：
  1. 删除需二次确认，取消不删除；
  2. 删除后题库列表不再含该题；
  3. 引用该题的未发布问卷在编辑界面显示"题目缺失"标记并可重新选题；
  4. 已发布问卷不受影响（快照，作答/导出正常）。

#### FR-36 从题库选择启用题目

- 需求：研究内"编辑问卷"：展示本人题库（可按题型筛选），研究者勾选题目加入问卷（按勾选顺序追加）；研究已发布时编辑入口关闭。
- 来源用例：UC-24 主流程 1–3 + 备选 3a。
- 验收标准：
  1. 可勾选加入的题目均来自本人题库；
  2. 加入顺序与勾选顺序一致；
  3. 已发布研究进入"编辑问卷"时入口关闭并提示问卷不可修改（锁定规则）；
  4. 加入的题目在问卷列表中可继续编排（FR-37）。

#### FR-37 调整启用题目顺序与移除

- 需求：研究者调整问卷中启用题目的顺序（线性）或移除题目；变更随问卷保存。
- 来源用例：UC-24 主流程 4。
- 验收标准：
  1. 顺序调整与移除正确生效且可撤销（编辑器撤销可用）；
  2. 移除后该题不再参与运行（不在问卷中）；
  3. 变更未保存时离开有提示（防丢失）。

#### FR-38 配置分支跳转逻辑

- 需求：研究者为每题选项配置流向（下一题/跳转指定题），默认下一题；跳转目标非法（未启用/不存在/死循环/不可达）时保存被阻止并定位提示。
- 来源用例：UC-25。
- 验收标准：
  1. 每个选项可独立配置流向；
  2. 未配置的选项默认流向下一题；
  3. 非法目标（未启用题目、跳转形成死循环/不可达）保存被阻止并给出定位；
  4. 规则随问卷保存（FR-39），运行时按规则路由（与 M5 问卷运行联动测试）。

#### FR-39 保存问卷

- 需求：保存问卷时校验启用题目存在且来自本人题库、跳转目标均为启用题目且无死循环/不可达；异步保存带状态提示。
- 来源用例：UC-26。
- 验收标准：
  1. 保存后重新进入（FR-36）编排一致；
  2. 引用缺失（题目被删）或跳转非法时保存被阻止并定位；
  3. 保存期间可继续编辑（异步，状态提示）；
  4. 问卷草稿与信息流草稿独立保存（互不影响）。

#### FR-40 问卷试答（v1 不做）

- 需求：研究者以被试者身份试答问卷草稿验证流程。
- 状态：**v1 不做**——原因与转正条件见 UC-27；保留本条供范围追溯。
- 来源用例：UC-27。
- 验收标准：v1 不适用；转正后补充（跳转按规则执行、必答校验、提交完成）。

### M5 参与会话与问卷运行

> 本模块为**被试者端公开流程**（经匿名 token 进入，不适用研究者 JWT 横切）；问卷在浏览结束后呈现（无浏览前问卷/穿插）；行为与眼动采集（M6）并行运行。

| 用例 | 对应 FR |
|---|---|
| UC-28 进入研究并创建会话 | FR-41 进入研究并创建会话 |
| UC-29 知情同意 | FR-42 知情同意 |
| UC-30 浏览模拟信息流 | FR-43 浏览模拟信息流（含浏览结束触发） |
| UC-31 作答问卷（含跳转运行时） | FR-44 作答问卷与跳转运行时 |
| UC-32 会话进度保存与续传 | FR-45 会话进度保存与恢复 |
| UC-33 会话结束与状态判定 | FR-46 会话状态判定与结束 |

#### FR-41 进入研究并创建会话

- 需求：被试者经参与链接（匿名 token）进入；系统校验研究存在且状态"收集中"、token 有效后创建匿名会话（匿名标识、设备信息、进入时间），进入同意步骤；无效/关闭场景给出明确提示且不创建会话。
- 来源用例：UC-28。
- 验收标准：
  1. 有效链接创建会话并进入 FR-42 同意步骤；
  2. 研究不存在/token 无效 → 无效链接提示，不创建会话；
  3. 研究未发布或已关闭 → 对应提示（如"本研究已结束"），不创建会话；
  4. 会话匿名（无邮箱/姓名等可识别信息）。

#### FR-42 知情同意

- 需求：进入后展示知情同意页（研究目的、采集内容、隐私说明、可随时退出）；同意前不启动任何采集；记录同意（时间戳）；不同意则结束会话且无数据残留。
- 来源用例：UC-29。
- 验收标准：
  1. 同意页内容完整展示（平台默认模板）；
  2. 同意前无任何行为/眼动采集启动（与 M6 联动验证）；
  3. 同意记录与会话关联（可追溯）；
  4. 选择不同意 → 会话结束（状态：放弃），无数据残留。

#### FR-43 浏览模拟信息流（含浏览结束触发）

- 需求：系统渲染 published 信息流快照（桌面端）供被试者浏览；采集并行运行（M6）且不阻塞浏览；浏览结束前问卷不可见；浏览结束支持两种触发——① 被试者点击"完成浏览"进入问卷；② 研究设定的浏览时长到时自动进入（**采用方式/是否可配置待客户确认后裁剪**）。
- 来源用例：UC-30。
- 验收标准：
  1. 渲染内容与发布快照一致（非草稿）；
  2. 浏览期间行为/眼动采集正常且页面流畅（联动 M6 验收）；
  3. 浏览结束前问卷不可见；
  4. 按最终确认的触发方式：点击"完成浏览"或到时自动跳转，均正确进入问卷且浏览阶段数据已缓冲/上报（不丢失）。

#### FR-44 作答问卷与跳转运行时

- 需求：浏览结束后呈现问卷（published 快照）；被试者逐题作答（题型渲染、必答校验）；每题提交后按作答值执行跳转路由（默认下一题，或按选项跳转指定题）；全部应答题完成后确认提交，完整性校验通过后会话置"完成"并锁定；异常跳转目标有防御回退（记录异常）。
- 来源用例：UC-31。
- 验收标准：
  1. 必答题未答不能进入下一题并提示；
  2. 跳转路由按作答值正确执行（与 FR-38 规则一致，确定性——联动 NFR-15 测试）；
  3. 被跳过的题目不产生作答记录（符合跳转语义）；
  4. 提交后会话状态"完成"，问卷不可再作答；
  5. 提交中断（网络）可恢复重试（联动 FR-45），不产生重复作答。

#### FR-45 会话进度保存与恢复

- 需求：系统在阶段边界保存进度（同意完成、浏览完成、问卷逐题作答）；刷新/断线后经同一链接恢复至断点（浏览中断回到浏览阶段起点或记录点——实现时定；问卷中断回到当前题）；已答题目不重复作答；已完成/放弃会话不可恢复。
- 来源用例：UC-32。
- 验收标准：
  1. 刷新后从正确断点继续，已保存进度不丢失；
  2. 恢复后已答题目不重复出现、不重复入库；
  3. 已"完成/放弃"会话不可再进入作答；
  4. 恢复后行为/眼动数据仍关联同一会话（时间线连续，联动 NFR-19）。

#### FR-46 会话状态判定与结束

- 需求：系统维护会话状态（进行中/完成/放弃）——正常提交置"完成"并显示结束页；关闭页面尽力标记"放弃"；超过无活动时限置"放弃"；研究被关闭时进行中会话终止（不可继续提交）；状态供 M2 UC-12 进度展示只读引用。
- 来源用例：UC-33。
- 验收标准：
  1. 正常完成：提交后状态"完成"，显示结束页，不可再作答；
  2. 关闭页面/超时：状态正确置"放弃"（超时时长实现时定，记录最后活动时间）；
  3. 研究关闭后进行中会话不可继续提交并给出提示；
  4. 状态计数与 M2 UC-12 进度列表一致。

### M6 行为采集与眼动

> 本模块为**系统侧能力**（运行于参与端，桌面浏览器）；采集仅在同意后启动（FR-42）；**只采集原始数据、不做衍生计算**；眼动每帧采样；无"降级为行为日志"概念——眼动不可用仅跳过眼动，不阻塞任何环节（均见 M6 模块说明与客户确认）。

| 用例 | 对应 FR |
|---|---|
| UC-34 行为数据采集与可靠上报 | FR-47 行为数据采集 |
| UC-34 行为数据采集与可靠上报 | FR-48 本地缓冲与可靠上报 |
| UC-35 眼动标定 | FR-49 眼动标定 |
| UC-36 眼动数据采集与控件归因 | FR-50 眼动采集与控件归因 |
| UC-37 眼动可用性与质量记录 | FR-51 眼动可用性判定与质量记录 |

#### FR-47 行为数据采集

- 需求：同意后系统启动行为采集——监听原生事件（点击/鼠标移动/键盘/滚动/页面可见性变化），按事件类型节流采样（鼠标 10–20Hz 等）；每条事件记录事件类型、时间戳、坐标/负载、目标控件 data-widget-id、会话标识；记录为原始数据，不做衍生计算。
- 来源用例：UC-34 主流程 1–3、6。
- 验收标准：
  1. 采集数据项与客户确认清单一致（[D-5] 默认清单）；
  2. 采样频率符合设定（鼠标 10–20Hz）；
  3. 每条事件含会话标识与控件归因，无跨会话串数据（抽样核对）；
  4. 采集不阻塞浏览/作答（联动 NFR-18/22）。
  5. 记录内容为原始事件（无衍生指标混入）。

#### FR-48 本地缓冲与可靠上报

- 需求：事件先写入本地缓冲（IndexedDB）；批量上报后端（事件类型 + JSONB 负载按会话入库）；断线自动重试、恢复后补传；会话结束前尽力上报（失败数据保留，同会话恢复时补传）。
- 来源用例：UC-34 主流程 4–5 + 备选。
- 验收标准：
  1. 断网/刷新期间事件不丢失（缓冲）；
  2. 网络恢复后补传成功，库中记录与本地计数一致（无丢失）；
  3. 重复上报不产生重复记录（幂等去重，联动 NFR-21）；
  4. 上报不阻塞页面操作。

#### FR-49 眼动标定

- 需求：研究启用眼动且同意后进入标定页（9 点标定，界面由交互实现）；系统计算标定质量评分并记录；**无论质量是否达标均进入浏览并继续眼动采集**（质量仅作数据标识）；质量过低可提示重试（限次，非阻断）；摄像头不可用/权限被拒 → 跳过标定与采集并记录原因，不阻塞流程。
- 来源用例：UC-35。
- 验收标准：
  1. 标定流程可完成且质量评分记录与会话关联；
  2. 标定未通过/放弃标定后，眼动追踪仍继续运行（事件带低质量标识）；
  3. 无摄像头/权限被拒时流程不被阻塞（直接进入浏览与问卷）；
  4. 标定过程本身不产生注视采集数据。

#### FR-50 眼动采集与控件归因

- 需求：浏览期间**每帧**采集注视数据（优先 Web Worker，实现受阻主线程采样为技术回退）；将 gaze 坐标映射到页面元素（elementFromPoint → closest('[data-widget-id]')）；每条事件记录注视点像素坐标 (x,y)（视口坐标系）、命中控件、时间戳、会话标识、标定质量标识——坐标与归因同时保留；未命中内容记录"未命中内容"不丢弃；与行为事件同一缓冲/上报/入库链路。
- 来源用例：UC-36。
- 验收标准：
  1. 眼动事件按每帧采样产生，归因正确（命中控件与页面布局一致——抽样验证）；
  2. 每条眼动事件均含原始 (x,y) 像素坐标；未命中内容时坐标仍被记录、事件不丢弃；
  3. 原始摄像头视频不离开浏览器（网络层无视频上传）；
  4. 眼动与行为数据同一时间轴、同一会话关联（无跨会话串数据）；
  5. 采集不阻塞浏览。

#### FR-51 眼动可用性判定与质量记录

- 需求：系统检测眼动可用性（浏览器摄像头支持 + 权限授予）；可用则执行标定与每帧采集；不可用则跳过眼动采集（标定与注视采集均不执行）并记录原因到会话；无论可用与否，浏览/问卷等流程正常进行；会话质量记录（是否采集眼动、原因、标定质量）与会话数据一并持久化（供导出过滤）。
- 来源用例：UC-37。
- 验收标准：
  1. 眼动不可用时浏览、问卷、提交全部正常（无阻塞）；
  2. 不可用会话不产生任何眼动事件（无错误/伪造数据）；
  3. 会话质量记录完整（眼动是否采集、不可用原因、标定质量），导出时可区分。

### M7 数据导出

> 导出为 **v1 确定需求**、研究者获取研究数据的**唯一出口**；内容为**原始数据**（无衍生指标）；导出受归属约束（继承 FR-08——只能导出自己研究的数据）；含会话质量字段（眼动可用性/标定质量）供研究者过滤。

| 用例 | 对应 FR |
|---|---|
| UC-38 导出研究数据（研究级批量） | FR-52 发起研究级导出 |
| UC-38 导出研究数据（研究级批量） | FR-53 导出内容生成与完整性校验 |
| UC-38 导出研究数据（研究级批量） | FR-54 打包与下载 |
| UC-39 导出单个被试者数据 | FR-56 单被试者导出 |
| UC-40 导出任务管理 | FR-55 导出任务管理与状态 |

#### FR-52 发起研究级导出

- 需求：研究者从研究详情（或参与情况页）发起导出——数据类别选项（问卷作答/行为事件/眼动事件，默认全选）与范围（v1 默认全部会话）；发起后创建异步导出任务并返回任务状态。
- 来源用例：UC-38 主流程 1–4。
- 验收标准：
  1. 导出入口可用，类别选项可勾选；
  2. 发起后任务创建成功，状态为"进行中"，可查看进度（FR-55）；
  3. 只能对归属自己的研究发起导出（继承 FR-08）；
  4. 导出期间研究者可继续其他操作（异步不阻塞）。

#### FR-53 导出内容生成与完整性校验

- 需求：系统按会话流式读取原始记录，生成分文件 CSV（sessions / responses / behavior_events / gaze_events），内容仅为原始数据（无衍生指标）；执行完整性校验（会话记录完整、时间戳时序一致、无跨会话串数据）；校验不通过则任务失败并定位异常。
- 来源用例：UC-38 主流程 5–6 + 备选 4a。
- 验收标准：
  1. 各 CSV 与库中原始记录一致（抽样比对），无衍生指标混入；
  2. 眼动 CSV 含原始 (x,y) 坐标列（见 FR-50）；
  3. 会话质量字段（眼动是否采集/标定质量/不可用原因）出现在导出中，研究者可按质量过滤；
  4. 完整性校验能发现异常会话并使任务失败、给出定位（不导出脏数据）。

#### FR-54 打包与下载

- 需求：校验通过后将各 CSV 打包为 zip 供下载；zip 内含 README/schema 说明（列定义与编码说明）；下载链接有有效期（实现时定，如 24 小时）。
- 来源用例：UC-38 主流程 7、UC-40。
- 验收标准：
  1. 完成的任务可下载 zip，内含各 CSV 与说明文件；
  2. 下载链接过期后不可再下载（重新发起可获取）；
  3. 下载权限校验（仅归属研究者，继承 FR-08）。

#### FR-55 导出任务管理与状态

- 需求：研究者查看导出任务列表与状态（进行中/完成/失败及原因）；完成后下载；失败任务可查看原因并重新发起；（可选）清理历史任务。
- 来源用例：UC-40。
- 验收标准：
  1. 任务状态与实际执行一致（轮询/刷新后正确）；
  2. 失败任务展示可理解的原因；
  3. 完成任务可下载（联动 FR-54）；
  4. v1 至少支持"当前任务状态可见 + 完成后下载"（历史保留策略实现时定）。

#### FR-56 单被试者导出

- 需求：研究者从 UC-13 被试者详情页发起该会话的导出——经同一异步管道导出该会话的作答、行为/眼动事件与会话信息（含质量字段）；某类数据缺失的会话（如未采集眼动）在 zip 内省略对应文件并在 README 注明（不生成空文件误导）。
- 来源用例：UC-39。
- 验收标准：
  1. 导出内容严格对应该会话（无跨会话串数据）；
  2. 内容与该会话在研究级导出中的记录一致；
  3. 无眼动数据的会话：zip 内无 gaze CSV 且 README 注明原因（不产生空文件）；
  4. 归属约束（继承 FR-08）。

## 5. 非功能需求（NFR）

### M1 账号与权限

| 用例 | 对应 NFR |
|---|---|
| UC-01 研究者注册 | NFR-01 密码存储与强度 |
| UC-01 研究者注册 | NFR-03 防暴力破解与枚举 |
| UC-01 研究者注册 | NFR-04 账号数据最小化 |
| UC-02 研究者登录 | NFR-02 会话与令牌安全 |
| UC-02 研究者登录 | NFR-03 防暴力破解与枚举 |
| UC-04 访问受保护功能（权限控制，横切） | NFR-02 会话与令牌安全 |
| UC-05 研究者修改密码 | NFR-01 密码存储与强度 |

#### NFR-01 密码存储与强度

- 需求/标准：密码一律 BCrypt 哈希存储（成本因子实现时定，建议 ≥10）；任何接口、日志不输出明文密码或哈希；注册与修改密码共用同一强度规则（默认密码长度 ≥8 位，具体复杂度实现时定）。
- 来源用例：UC-01、UC-05。
- 验证标准：
  1. 检查数据库：密码列存 BCrypt 哈希（`$2a$/$2b$` 前缀），成本因子不低于设定值；
  2. 接口响应与运行日志中检索不到明文密码或哈希；
  3. 强度规则单测覆盖（<8 位、纯数字等边界用例被拒）。

#### NFR-02 会话与令牌安全

- 需求/标准：JWT 签名密钥经环境变量配置（不入库、不硬编码、不进版本库）；令牌默认有效期建议 2 小时（实现时定）；过期/伪造令牌访问受保护端点一律返回 401；会话续期策略实现时定（v1 可简单要求重新登录，不引入 refresh token）。
- 来源用例：UC-02、UC-04。
- 验证标准：
  1. 代码与版本库中检索不到签名密钥字面值（密钥仅来自环境变量）；
  2. 签发令牌的 exp 与设定有效期一致（实测差值在容差内）；
  3. 构造过期令牌请求受保护端点 → 401（自动化测试）。

#### NFR-03 防暴力破解与枚举（可选，实现时定）

- 需求/标准：登录接口建议限速或连续失败限制（如连续失败 N 次后延迟/锁定，具体策略实现时定）；注册接口防滥用手段（如验证码/邮箱验证）暂不引入，评估后定。
- 来源用例：UC-01、UC-02。
- 验证标准（若实现）：① 连续失败达到阈值后，后续尝试被延迟/拒绝且提示不泄露原因细节；② 阈值解除规则符合实现设定（自动化测试覆盖）。

#### NFR-04 账号数据最小化

- 需求/标准：账号仅存储必要字段（邮箱、密码哈希、角色、创建/更新时间等）；不采集额外个人信息。
- 来源用例：UC-01。
- 验证标准：
  1. users 表字段清单仅含约定字段；
  2. 注册/登录接口只接收约定字段（多余字段被忽略或拒绝——自动化测试）。

### M2 研究管理

| 用例 | 对应 NFR |
|---|---|
| UC-09 发布研究 | NFR-05 研究状态机一致性 |
| UC-09 发布研究 | NFR-08 发布原子性 |
| UC-09 发布研究 | NFR-09 参与链接 token 安全 |
| UC-10 关闭/重新开放研究 | NFR-05 研究状态机一致性 |
| UC-11 删除研究 | NFR-05 研究状态机一致性 |
| UC-11 删除研究 | NFR-07 删除级联一致性 |
| UC-12 查看参与总览与进度列表 | NFR-06 列表与进度查询性能 |

#### NFR-05 研究状态机一致性

- 需求/标准：研究状态仅允许按"草稿 → 收集中 ⇄ 已关闭 → 删除"迁移（草稿不可直接关闭；已关闭/收集中可删除需二次确认）；并发发布/关闭操作不产生竞态（实现手段：状态条件更新或行锁，实现时定）。
- 来源用例：UC-09/10/11。
- 验证标准：
  1. 非法状态迁移（如草稿直接关闭、已删除再发布）被拒绝并返回明确错误；
  2. 并发测试：两请求同时发布/关闭同一研究，最终状态一致且无中间态（自动化并发测试）。

#### NFR-06 列表与进度查询性能

- 需求/标准：研究列表与参与进度列表采用分页加载；数千会话量级下接口响应时间预算（如 <2s，具体预算实现时定）。
- 来源用例：UC-12。
- 验证标准：构造千级会话数据，实测列表接口分页响应在预算内（压测脚本）。

#### NFR-07 删除级联一致性

- 需求/标准：删除研究时关联数据（文档快照、会话、作答、行为/眼动事件）在同一事务内级联删除，不留孤儿记录，不出现部分删除。
- 来源用例：UC-11。
- 验证标准：删除后按 study_id 检索各关联表均无残留（自动化校验脚本）；模拟删除中途失败可回滚（无部分删除状态）。

#### NFR-08 发布原子性

- 需求/标准：发布操作（内容校验 → 快照固化 → 状态变更 → 链接生成）原子完成；任一步失败不遗留中间态，可安全重试。
- 来源用例：UC-09。
- 验证标准：故障注入（快照后状态更新前抛错）后，研究状态与数据一致（无"半发布"），重试可成功完成。

#### NFR-09 参与链接 token 安全

- 需求/标准：参与链接 token 采用密码学安全随机数（熵 ≥128 bit），不可猜测、不可枚举。
- 来源用例：UC-09。
- 验证标准：代码审查 token 生成实现（安全随机源）；实测相邻两次生成的 token 无规律可预测（抽样对比）。

### M3 信息流编辑

| 用例 | 对应 NFR |
|---|---|
| UC-14 新建/进入信息流编辑 | NFR-10 编辑器序列化往返一致性 |
| UC-15 向信息流添加内容 | NFR-11 编辑器交互性能 |
| UC-16 调整信息流结构与顺序 | NFR-11 编辑器交互性能 |
| UC-17 编辑控件属性 | NFR-11 编辑器交互性能 |
| UC-18 管理图片素材 | NFR-12 素材上传安全与限制 |
| UC-20 保存信息流草稿 | NFR-10 编辑器序列化往返一致性 |
| UC-20 保存信息流草稿 | NFR-13 草稿保存的服务端防线 |

#### NFR-10 编辑器序列化往返一致性（无损）

- 需求/标准：文档经"编辑器节点树 → 适配层 → 纯净文档格式 → 适配层 → 编辑器节点树"往返后，内容、属性与顺序完全一致（无信息丢失、无字段漂移）。
- 来源用例：UC-14/20。
- 验证标准：
  1. 覆盖各控件类型与嵌套结构的往返测试（含边界：空文档、超长文本、特殊字符）；
  2. 保存-重载-再保存的二次往返结果与首次一致（幂等）。

#### NFR-11 编辑器交互性能

- 需求/标准：编辑器常规操作（添加/选中/属性修改/排序）响应流畅（典型操作响应 <100ms 级，实现时定基准）；较大文档（≥50 个控件实例）下属性编辑不产生可感知卡顿。
- 来源用例：UC-15/16/17。
- 验证标准：以 50+ 控件文档执行操作序列的性能冒烟测试（记录操作耗时与帧率表现）；属性输入（如文案打字）无输入延迟。

#### NFR-12 素材上传安全与限制

- 需求/标准：素材上传仅接受白名单类型（jpg/png/webp）且大小 ≤5MB；服务端再次校验文件类型（不信任客户端声明）；存储使用随机文件名防路径穿越/覆盖。
- 来源用例：UC-18。
- 验证标准：
  1. 绕过前端直接上传非法类型/超限文件 → 服务端拒绝；
  2. 上传文件名含路径字符（../ 等）被安全处理（存储名与服务名无关）；
  3. 素材经正确 Content-Type 提供。

#### NFR-13 草稿保存的服务端防线

- 需求/标准：保存接口在服务端对文档做完整校验（控件类型存在于注册表、props 符合 schema、结构合法），不信任前端校验结果。
- 来源用例：UC-20。
- 验证标准：直接调用保存接口提交非法文档（未知类型/非法 props/畸形 JSON）→ 返回 4xx 且不入库（自动化测试）。

### M4 问卷（题库 + 问卷）

| 用例 | 对应 NFR |
|---|---|
| UC-23 编辑/删除题库题目 | NFR-14 问卷-题库引用与快照一致性 |
| UC-25 配置分支跳转逻辑 | NFR-15 跳转规则正确性与确定性 |
| UC-26 保存问卷 | NFR-15 跳转规则正确性与确定性 |
| UC-21/22/24 题库与问卷编辑 | NFR-16 题库与问卷编辑性能 |

#### NFR-14 问卷-题库引用与快照一致性

- 需求/标准：问卷发布时固化题目内容快照——此后题库中题目的修改/删除**不影响已发布问卷**（作答、导出均取快照内容）；未发布问卷的引用始终解析到题库最新内容；引用缺失在问卷端可识别。
- 来源用例：UC-23、UC-09（联动）。
- 验证标准：
  1. 发布后修改/删除被引用题目 → 已发布问卷作答流程与导出内容不变（自动化测试）；
  2. 未发布问卷引用题目编辑后，问卷端读取到新内容；
  3. 删除题目后，引用缺失状态可检测（问卷保存被阻止或标记，见 FR-35/39）。

#### NFR-15 跳转规则正确性与确定性

- 需求/标准：跳转规则校验完备（目标为启用题目、无死循环、无不可达分支）；运行时路由确定（同一作答序列永远得到同一路径）。
- 来源用例：UC-25/26、UC-31。
- 验证标准：
  1. 确定性测试：固定作答序列多次执行得到相同路径（自动化）；
  2. 死循环/不可达构造用例在保存与运行时均被正确识别处理；
  3. 跳转测试覆盖所有分支组合的关键路径（数据正确性测试套件）。

#### NFR-16 题库与问卷编辑性能

- 需求/标准：题库列表/检索在千题量级下响应在预算内（如 <2s，实现时定）；问卷编辑（启用上百题）操作流畅。
- 来源用例：UC-21/22/24。
- 验证标准：构造千题题库与百题问卷，实测列表检索与编辑操作耗时在预算内（压测脚本）。

### M5 参与会话与问卷运行

| 用例 | 对应 NFR |
|---|---|
| UC-28 进入研究并创建会话 | NFR-20 会话与数据隔离 |
| UC-29 知情同意 | NFR-20 会话与数据隔离 |
| UC-30 浏览模拟信息流 | NFR-18 参与端性能与流畅度 |
| UC-31 作答问卷（含跳转运行时） | NFR-17 会话状态机一致性、NFR-18 参与端性能与流畅度 |
| UC-32 会话进度保存与续传 | NFR-19 会话恢复的数据一致性 |
| UC-33 会话结束与状态判定 | NFR-17 会话状态机一致性 |

#### NFR-17 会话状态机一致性

- 需求/标准：会话状态仅按合法路径流转（进入 → 同意 → 浏览 → 作答 → 完成；各阶段可因放弃/超时/研究关闭进入"放弃"）；提交与作答接口幂等（重复提交/重复上报不产生重复记录）；并发状态变更（提交与研究关闭同时发生）结果一致。
- 来源用例：UC-28~33。
- 验证标准：
  1. 非法状态流转被拒绝（如未同意即作答、完成后再作答）——自动化测试；
  2. 同一提交请求重复发送只生效一次（幂等键或状态校验）；
  3. 并发测试：提交与研究关闭并发发生时最终状态一致且无中间态。

#### NFR-18 参与端性能与流畅度

- 需求/标准：参与端为**桌面端轻量页面**——信息流加载时间预算（如 <3s，实现时定）；浏览滚动与问卷逐题切换流畅（不因采集（M6）产生可感知卡顿）；参与端不加载编辑器等重型代码（包体积约束）。
- 来源用例：UC-30/31。
- 验证标准：性能冒烟——发布长信息流（如 30 帖）实测加载与滚动帧率；问卷逐题切换无延迟；包体积检查（无编辑器库依赖）。

#### NFR-19 会话恢复的数据一致性

- 需求/标准：断线/刷新恢复后数据保持一致——已答题目不重复、事件不丢不重（缓冲补传幂等）、恢复前后数据时间线连续可关联同一会话。
- 来源用例：UC-32（联动 M6 缓冲）。
- 验证标准：断线注入测试——刷新/断网后恢复，断言：已答题仅一条记录；补传事件无重复（按事件幂等键去重）；事件时间线连续。

#### NFR-20 会话与数据隔离

- 需求/标准：匿名 token 与会话严格隔离——被试者仅能访问自己的会话（作答提交、进度恢复等端点校验会话 token 归属）；他人会话数据不可读写。
- 来源用例：UC-28/29/31/32。
- 验证标准：用会话 A 的 token 请求会话 B 的资源/提交 → 被拒绝（403/404）；token 无法枚举他人会话（自动化测试）。

### M6 行为采集与眼动

| 用例 | 对应 NFR |
|---|---|
| UC-34 行为数据采集与可靠上报 | NFR-21 采集数据不丢失不重复 |
| UC-34 行为数据采集与可靠上报 | NFR-22 采集性能开销 |
| UC-36 眼动数据采集与控件归因 | NFR-22 采集性能开销 |
| UC-36 眼动数据采集与控件归因 | NFR-23 事件完整性与归因正确性 |
| UC-36 眼动数据采集与控件归因 | NFR-24 隐私硬约束（原始视频不出浏览器） |
| UC-37 眼动可用性与质量记录 | NFR-23 事件完整性与归因正确性 |

#### NFR-21 采集数据不丢失不重复

- 需求/标准：采集链路（缓冲 → 批量上报 → 重试补传）保证数据不丢不重——事件带幂等键，重复上报去重；会话结束边界（尽力上报 + 同会话恢复补传）不产生丢失或重复。
- 来源用例：UC-34（联动 FR-45/48）。
- 验证标准：
  1. 断网注入后恢复，库中事件数与本地记录一致（无丢失）；
  2. 模拟重复上报同一批次 → 库中无重复记录（幂等去重测试）；
  3. 长时间会话（缓冲达上限）溢出策略生效（见 NFR-22），不静默丢弃未上报数据。

#### NFR-22 采集性能开销

- 需求/标准：采集（鼠标节流采样 + 眼动每帧采样）不产生可感知页面卡顿——眼动采样优先 Web Worker（主线程仅轻量转发）；本地缓冲有容量上限与溢出策略（实现时定，如条数/容量上限），长时间会话内存可控。
- 来源用例：UC-34/36（联动 NFR-18）。
- 验证标准：
  1. 浏览+采集并发时的性能冒烟：滚动与交互无长任务卡顿（主线程长任务时长预算）；
  2. 长时间会话（如 30 分钟浏览）内存增长受限（缓冲上限生效的观测测试）。

#### NFR-23 事件完整性与归因正确性

- 需求/标准：每条事件必填字段完整（类型/时间戳/会话标识；眼动事件含 (x,y)；点击/移动含坐标或负载）；归因（data-widget-id）与页面实际布局一致；全程无跨会话串数据。
- 来源用例：UC-34/36/37。
- 验证标准：
  1. 校验脚本：抽样事件必填字段完整率 100%（无缺字段记录）；
  2. 归因正确性抽样：构造已知布局，比较命中控件与预期（自动化）；
  3. 串会话检测：按会话分组统计，事件中无其他会话标识混入（自动化断言）。

#### NFR-24 隐私硬约束（原始视频不出浏览器）

- 需求/标准：摄像头原始视频永不传输/存储——网络请求仅允许约定的数据端点（事件上报等），无任何视频/图片流上传；采集仅在同意后与会话范围内运行。
- 来源用例：UC-36（联动 FR-42）。
- 验证标准：
  1. 网络层验证：参与端运行期间抓取/记录请求，断言无媒体流上传端点（自动化或审查清单）；
  2. 代码审查：无视频上传实现路径；摄像头流仅本地处理（canvas/worker）。

### M7 数据导出

| 用例 | 对应 NFR |
|---|---|
| UC-38 导出研究数据（研究级批量） | NFR-25 导出正确性 |
| UC-38 导出研究数据（研究级批量） | NFR-26 大数据量导出性能 |
| UC-38 导出研究数据（研究级批量） | NFR-27 导出文件规范 |
| UC-38/39/40 导出全流程 | NFR-28 下载安全 |

#### NFR-25 导出正确性

- 需求/标准：导出作为数据唯一出口，其结果必须与库中源数据一致（除明确声明的格式转换）——字段值、时间戳、会话归属均不改变；完整性校验不通过时**不放行导出**（宁可失败不可出错）。
- 来源用例：UC-38/39。
- 验证标准：
  1. 抽样比对：随机会话的导出行与库中记录逐字段一致（自动化比对脚本）；
  2. 构造异常数据（缺失记录的会话）→ 导出被拒绝并定位（校验测试）。

#### NFR-26 大数据量导出性能

- 需求/标准：导出采用流式生成（不整表载入内存）；百万级事件量级下任务可完成（不超时、内存峰值可控）；导出任务不阻塞其他请求与操作。
- 来源用例：UC-38/40。
- 验证标准：构造大研究（如百万级事件）发起导出，观测内存峰值与完成时间（在预算内）；导出进行中其他 API 响应正常（压测脚本）。

#### NFR-27 导出文件规范

- 需求/标准：CSV 采用 UTF-8 编码；字段含逗号/引号/换行时正确转义（不破坏列结构）；列结构稳定并随 zip 提供 schema/README 说明；文件名与列名命名规范一致。
- 来源用例：UC-38。
- 验证标准：
  1. 含特殊字符（逗号、引号、换行、中文）的数据导出后可被标准 CSV 工具正确解析（解析往返测试）；
  2. 编码校验（UTF-8 无乱码）；README 与实际列结构一致（自动化检查）。

#### NFR-28 下载安全

- 需求/标准：下载端点校验请求者身份与资源归属（仅研究者本人可下载自己研究的导出）；下载链接有效期（实现时定）；导出文件不落敏感路径（存储与提供路径隔离）。
- 来源用例：UC-38/39/40。
- 验证标准：
  1. 未登录/他人账号请求下载链接 → 被拒（401/403，自动化测试）；
  2. 过期链接不可用；
  3. 文件存储路径不可被直接遍历访问（路径不可猜测）。
