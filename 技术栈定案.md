# CS-42 技术栈定案

## 一、技术栈总览（速览表）

### 工程形态

| 层 | 定案 | 理由 |
|---|---|---|
| 工程形态(包管理) | Monorepo：pnpm workspace + Maven | 编辑器/参与端共享控件组件与类型；后端类型经 OpenAPI 契约生成 |

### 前端（浏览器内实现）

| 层 | 定案 | 理由 |
|---|---|---|
| 前端框架 | React 18 + TypeScript (JavaScript) + Vite | 前端成员无 TS/JS 经验，存在学习成本（JS 相对较低）；若 TS 影响进度可回退 JS（见 2.2 类型策略） |
| 样式 | TailwindCSS（主题扩展统一把控） | 样式统一由 Tailwind 主题管理；平台差异用 CSS 变量切换 |
| 编辑器引擎 | **craft.js（@craftjs/core）已定案** + 自研外壳 | spike 验证：React 18 兼容、序列化闭环；自带拖拽/层级/历史 |
| 组件基础 | **Headless UI**（研究者端）；参与端用原生元素 | 提供下拉/弹层/标签页等交互组件的可访问性行为，样式仍用 Tailwind；可回退原生（见 2.2） |
| 表单/校验 | propSchema（纯数据声明）+ 原生 React 渲染 | propSchema 驱动属性面板自动生成（加控件不改面板代码）；渲染与轻量校验用 React 原生实现，避免库学习成本；react-hook-form + zod 降为可选（表单变复杂再引入）；发布前整体校验由后端 Bean Validation 兜底 |
| 眼动（参与端） | **WebGazer.js + Web Worker 采样**；回退：主线程采样 | 浏览器内眼动估计，原型已验证（9 点标定 + 质量评分 + 降级）；Web Worker 不可行则主线程采样；数据经事件上报落库 |
| 行为采集（参与端采集层） | 原生事件 + 节流 + IndexedDB 缓冲 + 批量上报 | 断网不丢、与渲染解耦 |
| Dashboard 组件（P1 可选） | TanStack Table（表格）+ Recharts（图表） | Dashboard 需要表格/图表时再引入 |

### 后端（Java 服务内实现）

| 层 | 定案 | 理由 |
|---|---|---|
| 后端框架 | **Java 17 + Spring Boot 3**（Spring MVC，已定案，见 2.3） | 团队有 Spring 熟手；强类型保障数据正确性 |
| 安全 | **Spring Security + JWT** | 研究者登录认证、RBAC 权限控制（审计范围待客户确认，见待定项） |
| ORM | Spring Data JPA（Hibernate）+ Flyway 迁移 | 随 Java 定案；JSONB 用 JsonNode 类型映射 |
| 数据库 | PostgreSQL 16（JSONB 存文档与事件） | 与既有设计一致，灵活结构天然契合 |
| 参数校验 | Bean Validation（jakarta.validation） | API 入参校验与发布前完整性检查；与前端轻量校验各管边界（见 2.3） |
| 数据导出 | Apache Commons CSV（流式）+ ZipOutputStream | 大数据量导出不阻塞（导出管道在 Java 侧） |
| 开发体验 | spring-boot-devtools（热重载） | 缓解编译/重启循环对迭代速度的影响 |

### 工程化与衔接（前后端共同）

| 层 | 定案 | 理由 |
|---|---|---|
| 测试 | Vitest + RTL（前端）/ JUnit 5 + MockMvc + Testcontainers（后端）+ Playwright（E2E） | 每 sprint 带冒烟测试；Testcontainers / Playwright 为可回退项：无法实现可不做，退化为手动验证 |
| API 契约 | springdoc-openapi（OpenAPI 3）→ openapi-typescript 生成前端 TS 类型 | 前后端类型共享 |
| 部署 | Docker Compose + GitHub Actions CI | 本地一键起，CI 自动验证；GitHub Actions CI 为可回退项：无法实现可不做，退化为 push 前本地自跑 lint/test/build |

**待定项（依赖客户确认后定案，不阻塞其余各层）**：

| 项 | 候选/默认 | 依赖 | 说明 |
|---|---|---|---|
| 流程可视化（P1） | React Flow（候选） | 客户确认问卷需要条件分支且偏好可视化配置 | 问卷分支逻辑的流程图视图；若需求简单或客户无偏好，可砍掉，分支用表单配置 |
| 问卷题型库范围 | 默认常用题型集：单选/多选/Likert/开放文本/排序/矩阵/滑块 | 客户答复问卷用途与题型需求 | 影响问卷控件注册表规模，不影响技术选型（沿用 widgets 机制） |
| 问卷引擎规则模型（后端） | 分支/随机化/嵌入数据的 DSL 与存储结构 | 问卷模式（题库随机抽题 vs 固定题目多套模板）定案 | 在 backend survey 域内设计；技术载体（Spring Boot）已定，不受影响 |
| 审计范围（安全） | 记录研究者关键操作（发布/导出/删除等）、保留时长待定 | 客户确认安全/审计需求 | 实现机制已定（Spring Security 身份上下文 + audit_logs 表）；范围确认后补充 |

## 二、分层决策明细

### 2.1 工程形态：Monorepo（前端 pnpm + 后端 Maven）

**定案：单仓库（Monorepo）管理全部代码。前端用 pnpm 做包管理，后端用 Maven 做依赖管理与构建。**

- **结构**：
  ```
  frontend/         前端服务
    researcher-dashboard/  研究者端应用（pnpm）
      ├── 编辑器（信息流 / 问卷搭建，craft.js）
      ├── 数据日志与导出
      └── 研究 / 参与者管理
    participant/            参与者应用（pnpm）
      ├── 信息流浏览 + 问卷作答
      └── 行为 / 眼动采集
  backend/          后端服务（Maven，Spring Boot 单体应用）
    ├── 研究管理：研究 CRUD / 发布 / 关闭、邀请链接、认证与权限、审计日志
    ├── 内容与问卷：feed / 问卷定义的存储与版本（draft/published）、问卷实例生成与作答
    ├── 会话与行为：参与者会话（token / 续传）、行为 / 眼动事件接收与入库
    └── 数据导出：研究结果与行为数据 CSV / zip（含可选指标计算）
  packages/          前端共享层（pnpm）
    shared/         类型 + zod schema（前端共用 + 生成的 API 契约类型）
    widgets/        控件注册表 + 控件渲染器（dashboard 预览 / participant 共用）
    ui/             共享基础组件
  ```

- **pnpm 是什么**：pnpm 是前端（npm 生态）的包管理器，提供 workspace 多包支持——把仓库里多个 JS/TS 包当作一个整体来安装和管理依赖。

- **pnpm 的作用**：① 安装 React、craft.js、TailwindCSS 等依赖（全局只存一份，各包硬链接共享，装得快、省空间）；② 让 `researcher-dashboard`、`participant` 与 `packages/*` 在仓库内**直接互相引用**——widgets 控件和 shared 类型只需一份代码，两个应用共用，不复制、不发布。它保证 dashboard 与 participant 使用**同一份** widgets 控件（编辑器预览 = 参与者所见的一致性由此而来）。

- **Maven 是什么**：Maven 是 Java 生态的标准构建与依赖管理工具——用 `pom.xml` 声明依赖，负责拉取、编译、测试、打包全流程。

- **Maven 的作用**：从 Maven Central 拉取 Spring Boot 全家桶及第三方依赖；执行编译、测试、打包（产出可运行 jar）。后端只此一个工程，Maven 单模块即可，无需 workspace 机制。

- **两者的衔接点**：前后端**不共享代码**，通过 OpenAPI 契约通信（后端 springdoc 生成契约 → 前端 openapi-typescript 生成类型，见 2.3）。

- **后端组织方式**：单个 Spring Boot 应用，按上述四个功能域分包（Java 包）

### 2.2 前端

- **框架：React 18 + Vite**
  - 介绍：React 是组件化 UI 库（界面拆成组件、数据变化自动更新界面）；Vite 是构建工具（开发服务器秒级热更新、上线打包静态文件）。
  - 作用：两个前端应用（researcher-dashboard / participant）都以此为基础；组件化与"编辑器预览 = 参与端渲染"的共享组件架构天然契合。
- **语言与类型策略：TypeScript（可回退 JavaScript）**
  - 介绍：TypeScript 是加了静态类型的 JavaScript，写错类型编译期报错、编辑器有自动补全；JavaScript 无类型但学习曲线更平缓。
  - 作用：shared/widgets 的核心契约类型（`WidgetDef`、`FeedDoc`）依赖 TS 定义，是"编辑器 ↔ 参与端 ↔ 文档模型"一致性的基础。
  - **回退点**：前端成员无 TS/JS 经验；若 TS 学习成本影响进度 → 全 JS（Vite 原生支持 jsx/js，降级成本低），代价是失去类型检查；无论 TS/JS，核心契约类型由会 TS 的成员维护。决策时机：Sprint 3 前试运行一周后定。
- **编辑器引擎：craft.js（@craftjs/core）**
  - 介绍：React 的 headless 页面编辑器框架——提供编辑器引擎（节点树、拖拽、选中、撤销重做、序列化），不提供任何现成控件。
  - 作用：研究者端编辑器应用内使用，控件注册为 craft.js 自定义组件；**参与端不加载 craft.js**（只渲染发布的 JSON）。
- **样式：TailwindCSS（主题扩展）**
  - 介绍：原子化 CSS 框架——用现成类名直接拼样式，配合任意值精确还原平台细节（`rounded-[999px]` 等）。
  - 作用：颜色/字体/间距等设计值统一注册在 Tailwind 主题中（界面代码摸不到裸颜色）；**平台差异（theme-x / theme-ig）用 CSS 变量组切换**——组件只消费变量名、不写死值，控件的 `styleId` 属性决定套用哪组变量；设计数值由交互同学提供。
- **组件基础：Headless UI**
  - 介绍：无样式组件库——提供下拉、弹层、标签页等复杂交互组件的"行为"（键盘操作、焦点管理、无障碍语义），外观完全由 Tailwind 自己写。
  - 作用：研究者端用于属性面板下拉（styleId 切换）、控件库弹层、设置标签页等；**参与端不引库**（原生 radio/button/select 够用，保持轻量）。
  - **回退点**：参与端默认原生元素；研究者端若学习成本影响进度，同样回退原生 + 少量自写交互。
- **属性面板与表单：propSchema 声明 + 原生 React 渲染**
  - 介绍：propSchema 是每个控件在注册表里声明的"可编辑属性说明书"（纯数据：每个属性叫什么、什么类型、有哪些选项）；属性面板是一个通用组件，**按选中控件的 propSchema 自动渲染**对应输入控件（select/number/boolean…）。
  - 作用：加一个新控件只需注册 renderer + 一份 propSchema，面板代码零改动；渲染用原生 React（useState/受控组件），轻量校验手写；react-hook-form + zod 降为可选（表单复杂再引入）；发布前整体校验由后端 Bean Validation 兜底。
- **眼动追踪（参与端）：WebGazer.js + Web Worker 采样**
  - 介绍：WebGazer.js 是开源浏览器眼动估计库（MIT）——用摄像头画面做注视点估计，无需专用硬件；**Web Worker 是浏览器提供的"后台线程"机制：让 JS 跑在独立于页面渲染的线程上**，主线程（渲染 + 交互）与 worker（高频采样）互不阻塞。实现方式：主线程只做轻量转发（WebGazer 每帧坐标 → postMessage），眼动坐标的采样、打时间戳、缓冲（~30Hz）全部在 worker 里完成，避免高频采集阻塞页面渲染导致卡顿。
  - 作用：参与端信息流浏览时采集"被试者看了哪里"（原型已验证：9 点标定、标定质量评分、注视点采样）；gaze 坐标经元素映射（`elementFromPoint` + `data-widget-id`）对应到具体控件，随行为数据一并上报落库。
  - **技术降级**：Web Worker 实现不可行 → 主线程采样
- **数据表格（Dashboard，P1 可选）：TanStack Table**——研究者端数据浏览页需要表格时再引入。
- **图表（可选）：Recharts**——参与度概览等可视化需要时再引入。

### 2.3 后端

**定案：Java 17（LTS）+ Spring Boot 3（Spring MVC + Spring Data JPA）；安全、校验等为独立技术条目（见下）**

- **选型依据**：
  1. 满足团队现有能力，熟手先搭骨架，把非 Java 成员学习成本控制在最小（Node + TS、Python FastAPI 作为备选被否决的主因即团队能力与类型共享收益不抵成本）；
  2. Spring Security 对 RBAC + 审计（审计范围待客户确认，机制本身成熟）比在 Node 里自组认证更稳；
  3. 强类型 + 事务管理对"研究数据正确性"（随机化/分支/导出的一致性）更有保障；
  4. 前后端类型共享使用 OpenAPI 契约：springdoc 生成 OpenAPI → openapi-typescript 生成前端 TS 类型。
- **各技术介绍与作用**：
  - **语言与框架：Java 17（LTS）+ Spring Boot 3**
    - 介绍：Java 是强类型 JVM 语言（17 为 LTS 长期支持版，Spring Boot 3 的兼容基线）；Spring Boot 是 Java 后端的事实标准框架——自动配置 + 内嵌服务器，开箱即用。
    - 作用：承载全部后端功能域（研究管理 / 内容与问卷 / 会话与行为 / 数据导出）；强类型与事务管理保障"研究数据正确性"（随机化/分支/导出的一致性）。
  - **Web 层：Spring MVC**
    - 介绍：Spring 的 MVC 框架，用注解声明 REST 接口。
    - 作用：提供全部 API 端点（研究 CRUD、文档存取、事件接收、导出等），是前后端 OpenAPI 契约的生成源头。
  - **安全框架：Spring Security + JWT**
    - 介绍：Spring 官方安全框架（"安保系统"），JWT 是它查验的凭证（"身份证"）——提供认证流程、授权规则、过滤器链、身份上下文；JWT 的验签过滤器是过滤器链中的一个环节，两者不是二选一。
    - 作用：① 承载研究者登录认证（密码 BCrypt 校验 → 签发 JWT → 每请求验签放行）；② RBAC 授权——URL 规则 + 方法级注解（`@PreAuthorize`），只有 researcher 角色能发布/删除/导出；③ 身份上下文（`SecurityContextHolder`）——"研究者只能操作自己的研究"的资源归属校验与审计都建立在它之上；④ CORS/CSRF/会话策略统一配置。
    - 审计日志：实现机制已定（身份上下文 + audit_logs 表）；**记录哪些操作、保留多久待客户确认**（见一、待定项）。
  - **持久化：Spring Data JPA + Hibernate**
    - 介绍：Java 的 ORM 标准实现，用实体类映射数据库表。
    - 作用：业务表（studies、documents、events 等）的读写；JSONB 字段用 Hibernate 6 的 JsonNode 类型映射——文档结构演进不改表。
  - **数据库迁移：Flyway**
    - 介绍：数据库版本管理工具（"数据库的 Git"）——schema 变更写成带版本号的脚本。
    - 作用：团队各环境与演示服务器跑同一套迁移脚本，表结构可复现、可回滚、防漂移；新增数据项 = 新增一个版本脚本。
  - **参数校验：Bean Validation**
    - 介绍：Java 标准校验框架（jakarta.validation），用注解声明字段规则。
    - 作用：API 入参校验与发布前完整性检查——数据正确性的最后防线（前端为轻量校验，两套各负责边界）。
  - **API 文档与契约：springdoc-openapi**
    - 介绍：自动生成 OpenAPI 3 规范（openapi.json），自带 Swagger UI 可视化调试页。
    - 作用：① 人看——接口文档、调试、验收演示；② 机器用——前端 openapi-typescript 据此生成 TS 类型，是前后端契约的唯一来源。
  - **开发体验：spring-boot-devtools**
    - 介绍：热重载工具。
    - 作用：缓解 Java 编译/重启循环对迭代速度的影响。
- **影响范围**：API 层、ORM、校验、导出（Apache Commons CSV）、安全（Spring Security）。

### 2.4 数据库

- **数据库：PostgreSQL 16**
  - 介绍：开源关系型数据库，核心特性 JSONB（存 JSON 的二进制列类型）。
  - 作用：全项目唯一数据存储；JSONB 支撑灵活结构文档的存储（见下两条）。
- **JSONB 文档存储**
  - 介绍：`feed_documents` / `survey_definitions` 以 JSONB 列存整份文档（draft/published 双版本快照）。
  - 作用：灵活结构 + 版本化天然支持——文档结构随控件演进不用改表；ORM 侧由 JsonNode 承接（见 2.3）。
- **事件表设计：事件类型 + JSONB 负载**
  - 介绍：`behavior_events` / `gaze_events` 每行 = "事件类型"字段 + "负载"JSONB。
  - 作用：新增数据项不改表结构；按类型过滤查询即可。
- **ORM 与迁移：Spring Data JPA + Flyway**
  - 介绍/作用：与 2.3 持久化、迁移条目一致——实体读写 + schema 版本化迁移。
- **索引**
  - 介绍：按 study_id / session_id / participant_id 建索引。
  - 作用：行为数据按会话/研究维度查询时避免全表扫描，保证导出与浏览性能。

### 2.9 工程化与衔接（前后端共同）

- **测试：Vitest + React Testing Library（前端）/ JUnit 5 + MockMvc + Testcontainers（后端）/ Playwright（E2E）**
  - 介绍：测试体系按"金字塔"分层——前端组件/单元测试、后端接口与集成测试、端到端测试。Vitest 是 Vite 生态的测试运行器（跑前端测试的底座）；React Testing Library（RTL）以"模拟用户操作"的方式测试组件行为；JUnit 5 是 Java 标准测试框架（后端底座）；MockMvc 不启动真实服务器、直接模拟 HTTP 请求测试 API；Testcontainers 在测试时用 Docker 启动真实 PostgreSQL 做集成测试；Playwright 驱动真实浏览器走完整用户旅程。
  - 作用：前端——控件组件（点赞交互）、编辑器操作、序列化适配层；后端——研究 CRUD / 发布 / 事件接收接口（MockMvc）、真实数据库行为与 Flyway 迁移（Testcontainers）；E2E——"编辑 → 发布 → 参与端完成 → 数据回收"全流程正确性（跨环节 bug 只能在这一层发现）。
  - 数据正确性专项：问卷引擎分支/随机化（seeded RNG）确定性测试——研究数据可复现性是验收重点。
  - 节奏原则：每 sprint 交付物带冒烟测试（核心路径快速验证），W11 只做全量回归。
  - **可回退项（无法实现可不做）**：Testcontainers——若 Docker 环境不可用，集成测试退化为连开发库手动验证；Playwright——若无法实现，E2E 退化为按脚本人工走查（编辑 → 发布 → 参与 → 数据回收）。两者不做不阻塞交付，质量由其余测试层 + 人工验证兜底。
- **API 契约：springdoc-openapi → openapi-typescript**
  - 介绍：后端用 springdoc-openapi 自动生成 OpenAPI 3 规范（openapi.json，自带 Swagger UI）；前端用 openapi-typescript 读取该规范生成 TypeScript 类型文件。
  - 作用：后端接口是契约的唯一事实源——接口变更后前端重新生成即同步，消灭手写类型漂移；前端获得编译期类型检查与自动补全（Java 后端下替代 tRPC 的标准做法）。
- **部署：Docker Compose + GitHub Actions CI**
  - 介绍：Docker Compose 用一份 docker-compose.yml 定义"前端 + 后端 + PostgreSQL + Flyway 迁移"整套服务并一键启动；GitHub Actions 在每次 push 后自动执行 lint、测试与构建。
  - 作用：本地与演示环境"一条命令起全套"、团队成员环境一致（杜绝"我本地是好的"）；CI 把质量检查自动化——问题在合并前暴露，而不是集成时才被发现。
  - 后端镜像：多阶段 Dockerfile（maven:3.9-eclipse-temurin-17 构建 → eclipse-temurin:17-jre 运行），控制镜像体积。
  - 演示环境：Docker Compose 单机部署、直接暴露端口访问（部署目标：学校服务器 / 云 / 本地演示，待客户确认）；环境划分 dev / staging（可选）/ production（演示）。
  - **可回退项（无法实现可不做）**：GitHub Actions CI——若无法实现，退化为"push 前本地自跑 lint/test/build"的团队约定；风险（有人漏跑）由 code review 兜底。

---

## 三、技术学习分工建议（按成员配置）

**团队配置**：软工 ×2（负责后端）｜交互 ×2（负责前端）｜网安 ×2（安全 + DevOps）｜算法 ×1（问卷引擎设计与算法）｜AI ×1（眼动集成、数据质量与参与端采集）

**图例**：● 必学（该技术的负责实现者）｜◐ 需了解（配合/联调即可）｜— 无需学习

| 技术 | 软工 ×2 | 交互 ×2 | 网安 ×2 | 算法 ×1 | AI ×1 |
|---|---|---|---|---|---|
| React 18 + TS/JS + Vite | ◐ | ● | — | — | ◐ |
| TailwindCSS + CSS 变量主题 | — | ● | — | — | — |
| craft.js + 编辑器外壳 | ◐ | ● | — | — | — |
| Headless UI | — | ● | — | — | — |
| propSchema 属性面板 | ◐ | ● | — | — | — |
| WebGazer.js + Web Worker（参与端） | — | ◐ | — | — | ● |
| 行为采集（参与端采集层） | ◐ | ◐ | — | — | ● |
| Java 17 + Spring Boot 3（含 MVC） | ● | — | ◐ | ◐ | — |
| Spring Security + JWT | ◐ | — | ● | — | — |
| Spring Data JPA + Hibernate / Flyway | ● | — | — | — | — |
| Bean Validation | ● | — | — | — | — |
| springdoc-openapi（契约生成） | ● | ◐ | — | — | — |
| PostgreSQL 16 / JSONB | ● | — | — | ◐ | — |
| Apache Commons CSV（导出） | ● | — | — | — | — |
| Vitest + React Testing Library | — | ● | — | — | — |
| JUnit 5 + MockMvc（Testcontainers 可回退） | ● | — | — | — | — |
| Playwright（E2E） | ● | ◐ | — | — | — |
| Docker Compose | ◐ | — | ● | — | — |
| GitHub Actions CI（可回退） | ◐ | — | ● | — | — |

**分工说明（表格依据）**：

1. **前端技术**（框架/样式/编辑器/组件/表单）的责任人为交互 ×2——researcher-dashboard 与 participant 两个应用的界面都由他们实现；
2. **眼动与参与端采集**以 AI 同学为主（WebGazer 原型已由他完成）；交互同学负责把标定流程/采集页嵌入参与端界面；
3. **后端技术**责任人为软工 ×2；**Spring Security + JWT 与 DevOps（Docker/CI）以网安 ×2 为主**；
4. **算法同学不强制写 Java**——以"问卷引擎 DSL 设计 + 随机化/校验规则文档"交付、由软工实现，故 Java 仅标 ◐；
5. 软工标 ◐ 的前端项主要是"序列化适配层 / 契约联调"需要；交互标 ◐ 的后端项主要是"阅读 OpenAPI 契约 / 生成类型使用"需要；
6. 所有 ● 项建议在 **Sprint 2–3 骨架阶段安排结对学习**（Java 由熟手带软工之外的网安/算法成员做安全与规则部分；前端由交互同学互相结对），避免开发期现学现卖拖慢 sprint。
